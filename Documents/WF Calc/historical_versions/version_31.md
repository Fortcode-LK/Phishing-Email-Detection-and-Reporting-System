"# Phase 6: Capacity Constraints & Benchmark Validation\n\nCurrently, our optimizer can find the highest theoretical DPS combination, but it assumes unlimited Mod Capacity. Phase 6 introduces real-world Warframe constraints (Capacity limits) and pits the Optimizer against a known community \"Meta\" build to validate evaluator accuracy.\n\n## Proposed Architecture\n\n### 1. Mod Capacity & Polarity Definitions\nWe will extend the existing types to support the concept of drain and polarity. While full slot assignment (Aura/Stance matching) will come later, we will build the foundation for it now.\n\n#### [MODIFY] `src/lib/engine/optimizer/optimizerTypes.ts`\n```typescript\nexport enum Polarity {\n  MADURAI = \"MADURAI\",\n  VAZARIN = \"VAZARIN\",\n  NARAMON = \"NARAMON\",\n  ZENURIK = \"ZENURIK\",\n  PENJAGA = \"PENJAGA\",\n  UMBRA = \"UMBRA\"\n}\n\n// InGameMod will now require `capacityCost: number` and optional `polarity: Polarity`.\n\nexport interface BuildRules {\n  maxSlots: number;\n  maxCapacity: number;\n  allowDuplicates: boolean;\n  basePolarities: Polarity[]; // The polarities natively on the weapon\n}\n```\n\n### 2. Capacity Validation Math\nWe will introduce a greedy bin-packing algorithm to calculate the cheapest possible cost of a build given the weapon's polarities.\n\n#### [NEW] `src/lib/engine/optimizer/capacity.ts`\n- Implement `calculateBuildCost(build: ParsedMod[], polarities: Polarity[]): number`.\n- **Logic**:\n  1. Sort the `build` mods by `capacityCost` descending.\n  2. Maintain a list of available `polarities`.\n  3. For each mod, try to find a matching polarity in the available list.\n  4. If a match is found, add `Math.ceil(mod.capacityCost / 2)` to the total cost and consume that polarity slot.\n  5. If no match is found, add `mod.capacityCost` to the total cost.\n  6. Return `totalCost`.\n\n### 3. Generator Constraint Enforcement\nThe generator must ensure that only legal builds are passed into the pipeline.\n\n#### [MODIFY] `src/lib/engine/optimizer/generator.ts`\n- Update `generateR
<truncated 1933 bytes>