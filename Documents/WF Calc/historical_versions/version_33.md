"# Phase 8: Stacking State Context\n\nThis phase will build the foundational architecture for complex combat synergies. The central goal is to expand the mathematical engine's awareness of the current combat environment, specifically \"How many unique status effects are on the enemy?\" and \"How many times has this stacking buff triggered?\"\n\n## Proposed Architecture\n\n### 1. Update `CombatState`\nWe will expand the contextual state passed to the engine.\n\n#### [MODIFY] `src/lib/engine/types.ts`\nExpand `CombatState` to include new stack parameters:\n```typescript\nexport type CombatState = {\n  statusTypesOnEnemy?: number;\n  galvanizedStacks?: number;\n  mercilessStacks?: number;\n  deadheadStacks?: number;\n  adaptationStacks?: number;\n  comboMultiplier?: number;\n};\n```\n\n### 2. Implement Condition Overload Family\nCondition Overload, Galvanized Aptitude, Galvanized Savvy, and Galvanized Shot all share the exact same mechanical archetype: `+Damage per unique status effect`.\n\n#### [NEW] `src/lib/engine/evaluator/effects/ConditionOverloadFamily.ts`\n- Register `EffectType.CONDITION_OVERLOAD`.\n- Retrieve `context.evaluationContext.combatState.statusTypesOnEnemy`.\n- Calculate `bonusDamage = modifier.parameters?.value * statusTypesOnEnemy`.\n- Return a `Modifier` stat change that injects `bonusDamage` as a standard `baseDamage` multiplier directly into the math engine.\n\n### 3. Implement Galvanized Stack Engine\nGalvanized mods have an on-kill mechanic that stacks up to a maximum limit (e.g., +30% Multishot on Kill, stacks up to 5x).\n\n#### [NEW] `src/lib/engine/evaluator/effects/GalvanizedStacks.ts`\n- Register `EffectFamily.GALVANIZED`.\n- Retrieve `context.evaluationContext.combatState.galvanizedStacks` (defaults to max stacks if `mode === \"MAX_STACKS\"`).\n- Multiply the parameter's `value` by the `galvanizedStacks` (capped at `modifier.parameters.maxStacks`).\n- Inject the resulting stats (`multishot`, `statusChance`, etc.) into the math engine.\n\n### 4. Parser Support for the New Mechanics\nWe
<truncated 1549 bytes>