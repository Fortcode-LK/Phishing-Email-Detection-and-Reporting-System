"# Companions Database Creation Plan\n\nThis plan outlines how we will extract the organic and standard mechanical companions (Kubrows, Kavats, Sentinels) while filtering out modular robots and hounds, as requested.\n\n## User Review Required\n\n> [!NOTE]\n> I have investigated the `Pets.json` and `Sentinels.json` datasets. \n> \n> `Sentinels.json` contains exactly what we want: the 17 standard sentinels (Carrier, Dethcube, Helios, etc.).\n> \n> `Pets.json` is a bit messy because it contains full organic pets (Kubrows/Kavats), full robotic pets (Moas/Hounds), *and* the individual modular components used to build them (Cores, Brackets, Mutagens, etc.).\n> \n> **My filtering strategy:**\n> I will write the script to explicitly **include** any pet whose name contains `Kubrow`, `Kavat`, `Predasite`, `Vulpaphyla`, `Charger`, or `Venari`. This will perfectly isolate all organic pets and cleanly leave out the Moas, Hounds, and all the modular robotic scrap parts.\n>\n> We will store them in a new `companions.db` using a schema very similar to the Warframe database (Health, Shield, Armor, Power, Polarities).\n>\n> Does this filtering strategy and schema sound good to you?\n\n## Proposed Changes\n\nWe will create a script `build_companions_db.py`.\n\n### Schema Design: `companions` Table\n\n| Column | Type | Description |\n| :--- | :--- | :--- |\n| `id` | INTEGER | Primary Key |\n| `uniqueName` | TEXT | Unique identifier |\n| `name` | TEXT | Display name (e.g., \"Carrier Prime\", \"Smeeta Kavat\") |\n| `category` | TEXT | Category (e.g., \"Sentinel\", \"Kavat\", \"Kubrow\") |\n| `description` | TEXT | Flavor text |\n| `health` | INTEGER | Base health |\n| `shield` | INTEGER | Base shield |\n| `armor` | INTEGER | Base armor |\n| `power` | INTEGER | Base energy/power pool (for abilities/precepts) |\n| `polarities` | TEXT | JSON string of innate polarities |\n| `isPrime` | BOOLEAN | Whether it is a Prime variant |\n\n### Script Logic\n1. Create `companions.db`.\n2. Parse `Sentinels.json` and insert all items, tagging their c
<truncated 729 bytes>