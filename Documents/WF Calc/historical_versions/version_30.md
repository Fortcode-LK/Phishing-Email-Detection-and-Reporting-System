"# Phase 5: Random Search Optimizer Prototype\n\nThe objective of this phase is to validate the entire architecture end-to-end by building the first working prototype of the **Auto-Optimizer**. We will build a random-search generator that takes a curated pool of mods, constructs legal loadouts, evaluates them, and ranks them by user preference.\n\n## Proposed Architecture\n\n### 1. In-Game Mod Representation & Rules\nBefore generating builds, we must formally define what a \"Mod\" is and what constraints govern a \"Build\".\n\n#### [NEW] `src/lib/engine/optimizer/optimizerTypes.ts`\n```typescript\nexport interface InGameMod {\n  id: string; // e.g., \"serration\"\n  name: string; // \"Serration\"\n  text: string; // Raw text to be fed into the parser\n  capacityCost?: number; // Ignored for now\n  polarity?: string; // Ignored for now\n}\n\nexport interface BuildRules {\n  maxSlots: number; // e.g., 8\n  maxCapacity: number; // e.g., 60\n  allowDuplicates: boolean; // e.g., false\n}\n```\n\n### 2. The Build Generator\nA module responsible for creating legal combinations of mods.\n\n#### [NEW] `src/lib/engine/optimizer/generator.ts`\n- Implement `generateRandomBuilds(pool: InGameMod[], count: number, rules: BuildRules): InGameMod[][]`.\n- It will randomly select 1 to `maxSlots` unique mods from the `pool` per build, ensuring `allowDuplicates = false`.\n- *Performance Note*: `parseMod` should be run on the `pool` *before* optimization, so the random generator operates on pre-parsed arrays of `Modifier[]` rather than repeatedly running regex operations 1,000 times.\n\n### 3. The Evaluation Pipeline Wrapper\nA single function that wraps the entire calculation flow, representing the fundamental unit of the Optimizer.\n\n#### [NEW] `src/lib/engine/optimizer/pipeline.ts`\n```typescript\nexport interface EvaluatedBuild {\n  build: InGameMod[];\n  score: number;\n  metrics: WeaponMetrics;\n  result: MasterCalculationResult;\n}\n\nexport function evaluateBuild(\n  build: InGameMod[], \n  baseStats: WeaponBaseStats, \n  con
<truncated 2069 bytes>