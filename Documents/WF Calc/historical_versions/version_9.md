"# Advanced Math Engine Architecture Plan\n\nBased on your architectural feedback, the math engine will be structured as a decoupled, condition-aware vector system. This ensures it can handle advanced Warframe mechanics (status weighting, conditional buffs, and beam weapons) seamlessly in the future.\n\n## User Review Required\n\n> [!NOTE]\n> Thank you for the incredible architectural recommendations! I have completely restructured the engine to use **Damage Vectors** instead of scalars, implemented the **Condition System**, and decoupled the math into a dedicated **Formulas** directory for perfect unit testing.\n>\n> Please review this finalized architecture below. If you give the green light, we will move out of Planning Mode and start writing the code!\n\n## 1. The Core Data Structures\n\n### The Condition System\nTo future-proof for Galvanized mods, Arcane buffs, and Incarnons, parsed modifiers will not be simple key-value pairs. They will be structured to include conditions:\n```ts\n{\n  stat: \"CRIT_CHANCE\",\n  value: 1.35,\n  condition: \"ON_HEADSHOT\" | \"ALWAYS\" | \"ON_KILL\" // V1 will only process \"ALWAYS\"\n}\n```\n\n### The Damage Vector\nInstead of calculating a single `hitDamage` scalar, the engine will maintain a strict Damage Vector structure at all times, making armor calculation and status weighting possible later:\n```ts\ntype DamageVector = {\n  impact: number; puncture: number; slash: number;\n  heat: number; cold: number; toxin: number; electricity: number;\n  viral: number; corrosive: number; radiation: number;\n  gas: number; magnetic: number; blast: number;\n}\n```\n\n## 2. Decoupled Engine Architecture\n\nThe engine will reside in `src/lib/engine/` and execute as a modular, strictly functional pipeline.\n\n### `engine/parser/`\nTranslates raw mod text (`\"+165% Damage\"`) into the new conditional structures.\n\n### `engine/aggregator/`\nSums all parsed modifiers across 8 mod slots into an aggregated dictionary, keeping different conditions separated.\n\n### `engine/elemental/`\nCombi
<truncated 1337 bytes>