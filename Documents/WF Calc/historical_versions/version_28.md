"# Phase 3: Effect Archetypes & Context Expansion\n\nThe objective of this phase is to finalize the three foundational effect archetypes and introduce the `EvaluationContext`. This will prove the evaluator can handle temporary buffs (Growing Power) and stateful defensive mechanics (Adaptation), while integrating external environmental data (enemies/missions).\n\n## Proposed Architecture\n\n### 1. The Evaluation Context\nThe `CalculationContext` will be promoted to an `EvaluationContext` capable of representing the broader environment necessary for an optimizer to make comparative decisions.\n\n#### [NEW/MODIFY] `types.ts`\n```typescript\nexport type FactionType = \"GRINEER\" | \"CORPUS\" | \"INFESTED\" | \"CORRUPTED\" | \"MURMUR\" | \"OROKIN\" | \"SENTIENT\";\n\nexport type EnemyState = {\n  faction: FactionType;\n  level: number;\n  baseArmor: number;\n  currentArmor: number;\n  damageOutputVector?: DamageVector; // What damage the enemy deals to the player\n};\n\nexport type MissionState = {\n  steelPath: boolean;\n};\n\nexport type EvaluationContext = {\n  // loadout: LoadoutState; // (Placeholder for future Loadout representation)\n  enemy: EnemyState;\n  mission: MissionState;\n  activeConditions: Set<ConditionType>;\n};\n```\n*Note: `CalculationContext` in the master orchestrator will be replaced with `EvaluationContext`.*\n\n### 2. Archetype 2: Buff Effects (Growing Power)\nBuff effects produce `statChanges` rather than direct DPS contributions. \n\n#### [NEW] `GrowingPower.ts`\n- Implement `EffectType.GROWING_POWER` in the registry.\n- Evaluate Trigger: Check if `context.activeConditions.has(ConditionType.ON_STATUS_WEAPON)`.\n- Behavior: If triggered, return:\n  ```typescript\n  return {\n    contributions: [],\n    statChanges: [{\n      kind: ModifierKind.STAT,\n      stat: ModifierStat.ABILITY_STRENGTH,\n      value: 0.25,\n      condition: ConditionType.ALWAYS,\n      sourceSlot: modifier.sourceSlot,\n      sourceText: \"Growing Power Buff\",\n      equipScope: [ModifierCategory.WARFRAME],\n      targ
<truncated 2452 bytes>