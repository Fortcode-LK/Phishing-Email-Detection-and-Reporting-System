"# DamageVector Operations Implementation Plan\n\nBased on your request, I have reverted all the Master Calculator and Aggregator code. We will proceed strictly step-by-step. \n\nThe next foundational module to build is the **DamageVector Operations** layer. Because Warframe math relies entirely on manipulating 15-dimensional damage structures (Impact, Puncture, Slash, Heat, etc.), treating them as vectors rather than loose variables ensures safety and prevents code duplication in the future modules (Aggregator, Combiner, Faction Math).\n\n## User Review Required\n\n> [!NOTE]\n> Please review the proposed vector operations below. If these match the exact utility functions you envision using throughout the rest of the engine, approve this plan and I will build them!\n\n## The `vector.ts` Module\n\nThis module will reside in `src/lib/engine/formulas/vector.ts` (or `src/lib/engine/utils/vector.ts`) and contain pure, immutable mathematical operations.\n\n### 1. `getEmptyVector()`\nReturns a completely zeroed-out `DamageVector`. Essential for initializing baseline aggregations.\n\n### 2. `addVector(v1: DamageVector, v2: DamageVector)`\nTakes two damage vectors and returns a new vector where every corresponding damage type is summed. \n- **Use Case:** Adding Innate Damage to Modded Damage.\n\n### 3. `scaleVector(v: DamageVector, scalar: number)`\nMultiplies every property in the vector by a constant scalar.\n- **Use Case:** Applying the `+1.65` Base Damage (Serration) multiplier across all innate physical stats, or applying a Global Faction Multiplier.\n\n### 4. `sumVector(v: DamageVector)`\nReturns a single scalar representing the total numerical sum of all 15 damage types within the vector.\n- **Use Case:** Calculating the total raw hit damage to feed into the DPS formula, or computing the total innate damage pool to scale elemental mods.\n\n### 5. `mergeVector(base: DamageVector, overrides: Partial<DamageVector>)`\nCreates a new vector starting from `base`, but applies any specific keys found in `overrides`.\n- **Us
<truncated 368 bytes>