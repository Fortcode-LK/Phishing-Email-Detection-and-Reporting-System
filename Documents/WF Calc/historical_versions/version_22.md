"# Advanced Parameterized Mechanics Implementation Plan\n\nFollowing the insights from the Root Pattern Frequency Report, we are transitioning from simple STAT mappings into modeling parameterized game mechanics and states.\n\n## User Review Required\n> [!NOTE]\n> Regarding `Damage to <FACTION>` still showing up: I investigated this and found that the parser is working perfectly for Grineer/Corpus/Infested! The remaining 8 lines were specifically `Damage to Orokin` and `Damage to Sentients`, which simply weren't in the registry yet. They will be added.\n\n> [!TIP]\n> I completely agree with your assessment that we need to start modeling game systems rather than parsing text. I have outlined the architectural changes below to rename `metadata` to `parameters` and introduce the advanced parameterized effect families you suggested.\n\n## 1. Architectural Upgrades (types.ts)\n### Adding `parameters` to `Modifier`\nWe will rename the existing `metadata` field to `parameters` and use `Record<string, unknown>` to formally represent structured payloads for `ModifierKind.EFFECT`.\n\n### Adding `SQUAD` Scope\nWe will introduce `ModifierCategory.SQUAD` to properly model target scopes that affect the entire squad (e.g., Auras, passive team buffs).\n\n## 2. Second-Pass Condition Extraction (parser.ts)\nCurrently, `parser.ts` only looks for prefix conditions like `\"On Headshot: +120% Crit Chance\"`. \nWe will introduce a **suffix condition pass** to catch things like `\"Critical Chance when Aiming\"`.\n- It will strip `when Aiming` from the line and assign `ConditionType.ON_AIM` to the modifier.\n- This gracefully turns \"Critical Chance when Aiming\" into standard `CRIT_CHANCE` with the `ON_AIM` condition, avoiding an explosion of new enums.\n\n## 3. Parameterized Effect Families (registry.ts)\nInstead of hardcoding a dozen individual `EFFECT` or `STAT` ids, we will model the top root patterns as flexible parameterized families.\n*(Note: Because our `STAT_REGISTRY` operates via static objects, we will write out the regex ent
<truncated 1327 bytes>