"# Revised Regex Mod Parser Implementation Plan\n\nBased on your structural insights, the Mod Parser will be incredibly fault-tolerant. Instead of hardcoding conditionals, it will use a centralized **Regex Registry** mapped to strict TypeScript Enums. It will also gracefully fail on unknown lines rather than crashing the database import.\n\n## User Review Required\n\n> [!NOTE]\n> I have implemented all of your architectural recommendations: the `ModifierStat` Enum, the mapping registry, the handling of handling/companion stats, and the non-crashing `ParseResult` object.\n> \n> Review this finalized design. If it looks flawless, approve it and we'll begin the build!\n\n## 1. Strict Data Structures\n\nWe will replace the string-based stats with a bulletproof Enum to completely prevent typo-related bugs.\n\n```typescript\nexport enum ModifierStat {\n  BASE_DAMAGE = \"BASE_DAMAGE\",\n  FACTION_DAMAGE = \"FACTION_DAMAGE\",\n  CRIT_CHANCE = \"CRIT_CHANCE\",\n  CRIT_MULTIPLIER = \"CRIT_MULTIPLIER\",\n  STATUS_CHANCE = \"STATUS_CHANCE\",\n  STATUS_DURATION = \"STATUS_DURATION\",\n  FIRE_RATE = \"FIRE_RATE\",\n  MULTISHOT = \"MULTISHOT\",\n  MAGAZINE_CAPACITY = \"MAGAZINE_CAPACITY\",\n  RELOAD_SPEED = \"RELOAD_SPEED\",\n  AMMO_MAX = \"AMMO_MAX\",\n  PUNCH_THROUGH = \"PUNCH_THROUGH\",\n  PROJECTILE_SPEED = \"PROJECTILE_SPEED\",\n  ZOOM = \"ZOOM\",\n  RECOIL = \"RECOIL\",\n  ACCURACY = \"ACCURACY\",\n  SPREAD = \"SPREAD\",\n  // Companions\n  LINK_HEALTH = \"LINK_HEALTH\",\n  LINK_ARMOR = \"LINK_ARMOR\",\n  LINK_SHIELDS = \"LINK_SHIELDS\",\n}\n// Note: Elemental / IPS damage types will continue to map directly to the `DamageType` union we built.\n\nexport type ParseResult = {\n  modifiers: Modifier[];\n  unknownLines: string[]; // Logs unparseable lines (e.g., \"+5 Health/s\") without crashing\n};\n```\n\n## 2. The Regex Registry Architecture\n\nInstead of hundreds of `if` statements, the parser will iterate through a cleanly maintained registry object:\n\n```typescript\ntype StatMapping = {\n  regex: RegExp;\n  stat: Modif
<truncated 2189 bytes>