"# End-to-End Effect Evaluator Architecture\n\nThe goal is to implement the `EffectRegistry` and Evaluator pipeline to bridge the gap between parsed effects (e.g., `NamedEffectModifier`) and real mathematical outcomes (DPS). We will test this end-to-end with **Hunter Munitions**.\n\n## Proposed Changes\n\n### 1. Update Aggregator\nCurrently, the Aggregator drops any modifier that is not `ModifierKind.STAT`. \n\n#### [MODIFY] [aggregator.ts](file:///c:/Users/jkiri/Documents/WF%20Calc/calculator_app/src/lib/engine/aggregator/aggregator.ts)\n- Extend `AggregatedModifiers` to include `activeEffects: (FamilyEffectModifier | NamedEffectModifier)[]`.\n- Update `aggregateModifiers` to push `ModifierKind.EFFECT` entries into `activeEffects` (if their condition is met or is `ALWAYS`).\n\n### 2. Create the Effect Registry\nThe Effect Registry serves as the bridge between \"what the mod says\" and \"what the mod actually does.\"\n\n#### [NEW] [EffectRegistry.ts](file:///c:/Users/jkiri/Documents/WF%20Calc/calculator_app/src/lib/engine/evaluator/EffectRegistry.ts)\n- Define the `EffectDefinition` interface:\n  ```typescript\n  export interface EffectContext {\n    weapon: CalculatedWeapon;\n    aggregated: AggregatedModifiers;\n    addDPS: (source: string, value: number) => void;\n    // Future: addStat(), addCondition(), etc.\n  }\n\n  export interface EffectDefinition {\n    type: EffectType | EffectFamily;\n    // Using a class or object with an evaluate method:\n    evaluate: (modifier: EffectModifier, context: EffectContext) => void;\n  }\n  ```\n- Implement the `EffectRegistry` class with `.register()` and `.get()`.\n\n### 3. Implement Hunter Munitions\n#### [NEW] [HunterMunitions.ts](file:///c:/Users/jkiri/Documents/WF%20Calc/calculator_app/src/lib/engine/evaluator/effects/HunterMunitions.ts)\n- Register `EffectType.HUNTER_MUNITIONS`.\n- In `evaluate()`:\n  - Calculate Slash Proc Tick Damage: `ModdedBaseDamage * 0.35 * (1 + FactionDamage)^2`.\n  - Calculate Total Proc Damage: `Tick Damage * 6` (over 6 seconds).\n  - Cal
<truncated 1809 bytes>