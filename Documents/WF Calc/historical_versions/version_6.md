"# Next.js Application Setup & Mods Database Plan\n\nThis plan outlines how we will set up the foundation for the mod configuration calculator. \n\n## User Review Required\n\n> [!NOTE]\n> To answer your question: **No, we do not need Pandas!** Pandas is a Python data science library. Because we are moving to a Next.js (JavaScript/TypeScript) stack, our web server will use lightweight SQL queries to read our databases and standard JavaScript to perform the math calculations. This keeps the application incredibly fast and lightweight.\n>\n> **Before we start the Next.js app**, there is one final, crucial piece of data we need: **The Mods**. I am currently downloading `Mods.json` in the background. We need to parse this into a final `mods.db` so the Next.js app has mods to calculate with!\n>\n> Does this Next.js scaffolding plan and the final Mods database step look good to you?\n\n## Proposed Architecture\n\n### 1. Finalizing the Database Layer (`mods.db`)\n- **Action:** Create `build_mods_db.py`.\n- **Schema:**\n  - `id`, `uniqueName`, `name`, `type`\n  - `polarity` (e.g., Madurai, Naramon, Vazarin)\n  - `rarity` (Common, Uncommon, Rare, Legendary)\n  - `baseDrain`, `fusionLimit` (Max rank)\n  - `compatName` (What weapons/frames can equip this)\n  - `levelStats` (JSON array of the mod's specific stat buffs at each rank)\n- **Goal:** Parse `Mods.json` and move it into our `database_builder` folder alongside the others.\n\n### 2. Scaffolding the Next.js Application\n- **Action:** Run `npx create-next-app@latest wf-calc-app` to generate the application folder structure.\n- **Dependencies to Install:**\n  - `better-sqlite3` (A blazing-fast library allowing our Next.js backend to read our SQLite databases).\n- **Styling:** As per guidelines, we will use **Vanilla CSS** (`page.module.css` and `globals.css`) to build a bespoke, highly dynamic, glassmorphic UI.\n\n## Next Steps Upon Approval\n1. Execute `build_mods_db.py` to finalize the data layer.\n2. Initialize the Next.js application.\n3. Configure the Next.js API rou
<truncated 60 bytes>