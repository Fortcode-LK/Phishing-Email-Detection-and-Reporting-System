"# Phase 9: Melee & Stance Foundation\n\nThe goal of this phase is to expand the evaluation engine to natively support Warframe's most crucial melee scaling vectors: Combo Multiplier and Combo Count. By integrating these systems, we can natively simulate the two most iconic melee scaling mods—Blood Rush and Weeping Wounds—using the exact same `ScalingEffect` architecture developed in Phase 8.\n\n> [!NOTE]\n> This validates the generic `ScalingEffect` structure. If we can hook `Blood Rush` through the exact same logic pipeline as `Condition Overload` and `Galvanized Chamber`, the architecture is highly sound.\n\n## Proposed Changes\n\n### `src/lib/engine/types.ts`\n- **[MODIFY]** Expand `CombatState` to include `comboMultiplier?: number` and `comboCount?: number`.\n\n### `src/lib/engine/evaluator/effects/ScalingEffect.ts`\n- **[MODIFY]** Update the `ScalingEffect` evaluator to intercept `ScalingSource.COMBO_MULTIPLIER`. \n- Implement the Warframe standard math for combo scaling: `units = Math.max(0, comboMultiplier - 1)`. (e.g., a 12x combo provides 11 stacks of Blood Rush).\n\n### `src/lib/engine/evaluator/comboEstimator.ts`\n- **[NEW]** Create a dedicated `comboEstimator.ts` (paralleling `statusEstimator.ts`). \n- Default Heuristics:\n  - `FRESH_TARGET`: `comboMultiplier = 1`\n  - `SUSTAINED_FIRE`: `comboMultiplier = 8` (to represent prolonged combat build-up)\n  - `MAX_STACKS`: `comboMultiplier = 12`\n\n### `src/lib/engine/parser/registry.ts`\n- **[MODIFY]** Add Regex parsing patterns for:\n  - `Critical Chance stacks with Combo Multiplier` -> Maps to `ScalingSource.COMBO_MULTIPLIER` affecting `ModifierStat.CRIT_CHANCE`\n  - `Status Chance stacks with Combo Multiplier` -> Maps to `ScalingSource.COMBO_MULTIPLIER` affecting `ModifierStat.STATUS_CHANCE`\n\n## Verification Plan\n\n### Automated Tests\n1. Update `tests/scaling_effect.test.ts` to assert that `Blood Rush` correctly parses into the `ScalingEffect` flow.\n2. Assert that `EvaluationMode` directly controls the expected mathematical output of Blood Rus
<truncated 435 bytes>