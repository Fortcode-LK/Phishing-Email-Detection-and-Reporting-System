"# Regex Mod Parser Implementation Plan\n\nThe `ModParser` is the \"front door\" of the math engine. It is responsible for translating raw text from the SQLite database (e.g., `\"+165% Damage\"`) into strictly typed `Modifier` objects that the Master Orchestrator can calculate.\n\n## User Review Required\n\n> [!IMPORTANT]\n> Parsing natural language is inherently tricky. The proposed pipeline relies on a strict Regex Mapping table. \n> \n> Please review the **Stat Mapping** section closely. Did I miss any major keyword patterns that Warframe uses for mods?\n\n## 1. The Output Target\n\nThe parser must output an array of `Modifier` objects for each mod it processes. A single mod might have multiple effects (e.g., Malignant Force provides both Toxin and Status Chance), meaning one mod string maps to an *array* of `Modifier` objects.\n\n```typescript\ntype Modifier = {\n  stat: string; // e.g. \"BASE_DAMAGE\", \"toxin\", \"CRIT_CHANCE\"\n  value: number; // e.g. 1.65 (for +165%)\n  condition: ConditionType;\n  sourceSlot: number;\n};\n```\n\n## 2. The Parsing Pipeline\n\nThe parser will live in `src/lib/engine/parser/parser.ts` and follow a strict 4-step pipeline:\n\n### Step 1: Tokenization\nMods often contain multiple stats separated by newlines or commas. The parser will split the raw string into an array of individual stat lines.\n- **Input:** `\"+60% Toxin\\n+60% Status Chance\"`\n- **Tokens:** `[\"+60% Toxin\", \"+60% Status Chance\"]`\n\n### Step 2: Condition Extraction\nThe parser will check the beginning of the string for specific condition triggers using Regex, and strip the condition text from the string.\n- `\"On Headshot:\"` ➡️ `ConditionType.ON_HEADSHOT`\n- `\"On Kill:\"` ➡️ `ConditionType.ON_KILL`\n- No trigger found ➡️ `ConditionType.ALWAYS`\n\n### Step 3: Value & Sign Extraction\nThe parser will extract the number, the sign (`+` or `-`), and whether a `%` sign is present.\n- If `%` is present: Divide the value by 100 (e.g., `165` ➡️ `1.65`).\n- If no `%` is present: Keep the flat val
<truncated 1469 bytes>