Version 1: # Warframe Database Creation Plan\n\nThis plan outlines how we will extract the downloaded `Warframes.json` data, clean it, and structure it into a usable SQLite database specifically designed for a mod configuration calculator.\n\n## User Review Required\n\n> [!IMPORTANT]  \n> Please review the proposed database schema below. Since the primary goal is a mod configuration calculator, I have focused on the stats that mods affect (Health, Shields, Armor, Power/Energy, Sprint Speed, Polarities, Aura). \n> \n> Let me know if you would like me to include other fields (e.g., `abilities` details, crafting `components`, etc.) or if the current scope is perfect for the calculator.\n\n## Proposed Changes\n\nWe will create a Python script (`build_db.py`) that reads the downloaded `Warframes.json` and creates an SQLite database (`warframes.db`). \n\n### Schema Design: `warframes` Table\n\nThe database will contain a primary `warframes` table with the following schema:\n\n| Column | Type | Description |\n| :--- | :--- | :--- |\n| `id` | INTEGER | Primary Key |\n| `uniqueName` | TEXT | Unique identifier from the game data |\n| `name` | TEXT | The Warframe's display name (e.g., \"Ash\", \"Excalibur Prime\") |\n| `description` | TEXT | Flavor text / description |\n| `health` | INTEGER | Base health |\n| `shield` | INTEGER | Base shield |\n| `armor` | INTEGER | Base armor |\n| `power` | INTEGER | Base energy/power pool |\n| `sprintSpeed` | REAL | Base sprint speed |\n| `aura` | TEXT | Aura polarity (if any) |\n| `polarities` | TEXT | JSON string containing the list of innate polarities |\n| `isPrime` | BOOLEAN | Whether the Warframe is a Prime variant |\n\n*Note: Nested arrays like `polarities` will be stored as JSON-encoded strings for easy retrieval, which is standard practice for SQLite without needing an overly complex relational setup.*\n\n### Python Script (`build_db.py`)\n\nThis script will:\n1. Connect to `warframes.db`.\n2. Create the `warframes` table if it doesn't exist.\n3. Parse `Warframes.json`.\n4. Filter out any
Version 2: # Weapons Database Creation Plan\n\nThis plan outlines how we will extract and unify the `Primary.json`, `Secondary.json`, and `Melee.json` datasets into a unified `weapons.db` SQLite database for the mod configuration calculator.\n\n## User Review Required\n\n> [!IMPORTANT]\n> Because guns and melee weapons share some stats (Crit Chance, Status Chance) but have completely different unique mechanics (Reload Speed vs Combo Duration), I propose creating a **single, unified `weapons` table** where irrelevant fields are simply left empty (NULL) depending on the weapon category. \n> \n> Please review the schema below to ensure it captures all the attributes you want to calculate mod configurations for.\n\n## Proposed Changes\n\nWe will create a new Python script `build_weapons_db.py` to parse all three JSON files and compile them.\n\n### Schema Design: `weapons` Table\n\n| Column | Type | Description |\n| :--- | :--- | :--- |\n| `id` | INTEGER | Primary Key |\n| `uniqueName` | TEXT | Unique identifier |\n| `name` | TEXT | Display name (e.g., \"Soma Prime\", \"Nikana\") |\n| `category` | TEXT | `Primary`, `Secondary`, or `Melee` |\n| `type` | TEXT | Specific weapon type (e.g., \"Rifle\", \"Shotgun\", \"Pistol\", \"Dual Swords\") |\n| `criticalChance` | REAL | Base Critical Chance |\n| `criticalMultiplier` | REAL | Base Critical Multiplier |\n| `procChance` | REAL | Status Chance |\n| `fireRate` | REAL | Fire Rate (Guns) or Attack Speed (Melee) |\n| `totalDamage` | REAL | Total base damage |\n| `damage` | TEXT | JSON object detailing specific damage types (Slash, Impact, Puncture, Heat, etc.) |\n| `polarities` | TEXT | JSON array of innate mod polarities |\n| `attacks` | TEXT | JSON array of alt-fire or heavy attack stats |\n| `isPrime` | BOOLEAN | Whether it's a Prime variant |\n\n**Gun-Specific Fields:**\n| Column | Type | Description |\n| :--- | :--- | :--- |\n| `magazineSize` | INTEGER | Clip size |\n| `reloadTime` | REAL | Reload time in seconds |\n| `multishot` | INTEGER | Base multishot (usually 1, sometimes inn
Version 3: # Specialized Weapons Database Creation Plan
Version 4: # Exalted Weapons Database Creation Plan\n\nThis plan outlines how we will extract Exalted Weapons (like Excalibur's Exalted Blade or Mesa's Regulators) and structure them into a dedicated `exalted_weapons.db`.\n\n## User Review Required\n\n> [!NOTE]\n> I have located the Exalted Weapons! The `warframe-items` dataset does not have a dedicated `Exalted.json` file. Instead, the exalted weapons are hidden inside a large file called `Misc.json`. \n> \n> To extract them cleanly, I propose the following strategy:\n> 1. Read `Warframes.json` to extract the `exalted` uniqueName IDs from every Warframe.\n> 2. Read `Misc.json` and filter out only the items whose `uniqueName` matches the IDs we collected from the Warframes.\n> 3. Store these weapons in `exalted_weapons.db` using the same unified `weapons` schema we've been using.\n>\n> Does this strategy sound solid? \n\n## Proposed Changes\n\nWe will create a new Python script `build_exalted_db.py`.\n\n### Step 1: Collect Unique Names\nThe script will load `Warframes.json` and iterate over all 118 warframes. Whenever a warframe has an `exalted` array, it will add those `uniqueName` strings to a master list.\n\n### Step 2: Filter and Insert\nThe script will then load `Misc.json` and iterate through it. If an item's `uniqueName` exists in our master list, it will be parsed and inserted into `exalted_weapons.db` using the unified schema we used for all other weapons. \n\n### Schema\nWe will continue to use the unified `weapons` schema. Since exalted weapons are functionally either guns (Regulators, Dex Pixia) or melee (Exalted Blade, Valkyr Talons), the unified schema with nullable fields works perfectly here.\n\n## Verification Plan\n\n### Automated Tests\n- A `verify_exalted_db.py` script will query the total count of inserted exalted weapons.\n- It will verify specific samples like:\n  - **Exalted Blade** (Melee-style exalted weapon)\n  - **Regulators** (Gun-style exalted weapon)\n\n### Manual Verification\n- You can manually inspect `exalted_weapons.db` to confirm that a
Version 5: # Companions Database Creation Plan\n\nThis plan outlines how we will extract the organic and standard mechanical companions (Kubrows, Kavats, Sentinels) while filtering out modular robots and hounds, as requested.\n\n## User Review Required\n\n> [!NOTE]\n> I have investigated the `Pets.json` and `Sentinels.json` datasets. \n> \n> `Sentinels.json` contains exactly what we want: the 17 standard sentinels (Carrier, Dethcube, Helios, etc.).\n> \n> `Pets.json` is a bit messy because it contains full organic pets (Kubrows/Kavats), full robotic pets (Moas/Hounds), *and* the individual modular components used to build them (Cores, Brackets, Mutagens, etc.).\n> \n> **My filtering strategy:**\n> I will write the script to explicitly **include** any pet whose name contains `Kubrow`, `Kavat`, `Predasite`, `Vulpaphyla`, `Charger`, or `Venari`. This will perfectly isolate all organic pets and cleanly leave out the Moas, Hounds, and all the modular robotic scrap parts.\n>\n> We will store them in a new `companions.db` using a schema very similar to the Warframe database (Health, Shield, Armor, Power, Polarities).\n>\n> Does this filtering strategy and schema sound good to you?\n\n## Proposed Changes\n\nWe will create a script `build_companions_db.py`.\n\n### Schema Design: `companions` Table\n\n| Column | Type | Description |\n| :--- | :--- | :--- |\n| `id` | INTEGER | Primary Key |\n| `uniqueName` | TEXT | Unique identifier |\n| `name` | TEXT | Display name (e.g., \"Carrier Prime\", \"Smeeta Kavat\") |\n| `category` | TEXT | Category (e.g., \"Sentinel\", \"Kavat\", \"Kubrow\") |\n| `description` | TEXT | Flavor text |\n| `health` | INTEGER | Base health |\n| `shield` | INTEGER | Base shield |\n| `armor` | INTEGER | Base armor |\n| `power` | INTEGER | Base energy/power pool (for abilities/precepts) |\n| `polarities` | TEXT | JSON string of innate polarities |\n| `isPrime` | BOOLEAN | Whether it is a Prime variant |\n\n### Script Logic\n1. Create `companions.db`.\n2. Parse `Sentinels.json` and insert all items, tagging their c
Version 6: # Next.js Application Setup & Mods Database Plan\n\nThis plan outlines how we will set up the foundation for the mod configuration calculator. \n\n## User Review Required\n\n> [!NOTE]\n> To answer your question: **No, we do not need Pandas!** Pandas is a Python data science library. Because we are moving to a Next.js (JavaScript/TypeScript) stack, our web server will use lightweight SQL queries to read our databases and standard JavaScript to perform the math calculations. This keeps the application incredibly fast and lightweight.\n>\n> **Before we start the Next.js app**, there is one final, crucial piece of data we need: **The Mods**. I am currently downloading `Mods.json` in the background. We need to parse this into a final `mods.db` so the Next.js app has mods to calculate with!\n>\n> Does this Next.js scaffolding plan and the final Mods database step look good to you?\n\n## Proposed Architecture\n\n### 1. Finalizing the Database Layer (`mods.db`)\n- **Action:** Create `build_mods_db.py`.\n- **Schema:**\n  - `id`, `uniqueName`, `name`, `type`\n  - `polarity` (e.g., Madurai, Naramon, Vazarin)\n  - `rarity` (Common, Uncommon, Rare, Legendary)\n  - `baseDrain`, `fusionLimit` (Max rank)\n  - `compatName` (What weapons/frames can equip this)\n  - `levelStats` (JSON array of the mod's specific stat buffs at each rank)\n- **Goal:** Parse `Mods.json` and move it into our `database_builder` folder alongside the others.\n\n### 2. Scaffolding the Next.js Application\n- **Action:** Run `npx create-next-app@latest wf-calc-app` to generate the application folder structure.\n- **Dependencies to Install:**\n  - `better-sqlite3` (A blazing-fast library allowing our Next.js backend to read our SQLite databases).\n- **Styling:** As per guidelines, we will use **Vanilla CSS** (`page.module.css` and `globals.css`) to build a bespoke, highly dynamic, glassmorphic UI.\n\n## Next Steps Upon Approval\n1. Execute `build_mods_db.py` to finalize the data layer.\n2. Initialize the Next.js application.\n3. Configure the Next.js API rou
Version 7: # Core Math Engine Architecture Plan\n\nBefore we write any code, we need to map out exactly how the math will flow based on the official Warframe damage formulas scraped from the Wiki.\n\n## User Review Required\n\n> [!NOTE]\n> I have pulled the core damage formulas from the Warframe Wiki. Warframe math is notoriously complex because different multipliers apply at different stages (e.g., Base Damage vs Elemental Damage vs Faction Damage).\n>\n> **Open Question:** Do you want the engine to calculate **Status Effect Damage** (like Slash procs or Heat procs), or should we stick strictly to **Raw Hit Damage** (Burst DPS & Sustained DPS) for the initial MVP?\n>\n> Please review the mathematical formulas and engine structure below and let me know if you approve!\n\n## 1. The Mathematical Formulas\n\nBased on the game engine, here are the core formulas we will hardcode into our engine:\n\n- **Modded Base Damage:** `Base Damage * (1 + Sum of Base Damage Mods)`\n- **Multishot:** `Base Multishot * (1 + Sum of Multishot Mods)`\n- **Critical Chance:** `Base Crit Chance * (1 + Sum of Crit Chance Mods)`\n- **Critical Multiplier:** `Base Crit Multiplier * (1 + Sum of Crit Damage Mods)`\n- **Elemental Damage:** `Modded Base Damage * (Sum of Elemental Mods)`\n  - *Note: Elements combine in a specific order (Top-Left to Bottom-Right in the mod slots).*\n- **Total Raw Damage per Shot:** `(Modded Base Damage + Elemental Damage) * (1 + Faction Damage Mods)`\n- **Average Damage per Shot (with Crits):** `Total Raw Damage * (1 + Final Crit Chance * (Final Crit Multiplier - 1))`\n- **Burst DPS:** `Average Damage per Shot * Final Fire Rate * Final Multishot`\n\n## 2. Engine Pipeline Architecture\n\nWe will structure the engine inside our Next.js application as a robust TypeScript library (`src/lib/engine/`):\n\n### Phase A: Mod Parsing (`ModParser.ts`)\nThe `levelStats` from our database are raw text (e.g., `+165% Damage`). The parser uses Regular Expressions to identify the `StatType` and `Value`.\n- **Input:** `\"+165% Damage\"`\n- **
Version 8: # Advanced Math Engine Architecture Plan\n\nBased on the detailed damage formulas provided, we will build a highly accurate, deterministic TypeScript damage engine. This engine will correctly handle fractional multi-tier critical hits (orange/red crits), firing uptime, and correct modifier layering.\n\n## User Review Required\n\n> [!NOTE]\n> Thank you for providing the precise mathematical model! I have updated the pipeline to use the exact multi-tier critical logic and faction-layering mathematics you provided. \n>\n> The engine will accurately handle fractional overflow for orange/red crits using the `E[CM]` expected value logic, ensuring our Burst and Sustained DPS calculations are mathematically identical to community standards.\n>\n> Please review the updated advanced architecture below. Does this perfectly align with your vision for the core engine?\n\n## 1. The Advanced Mathematical Model\n\nWe will hardcode the following mathematical pipeline into our `DamageCalculator.ts`:\n\n### Base and Elemental Layer\n- **Modded Base Damage:** \\( BD_{mod} = BD \\times (1 + M_{BD}) \\)\n- **Elemental Damage:** \\( D_{elem} = BD_{mod} \\times M_{elem} \\)\n- **Hit Damage (Post-Faction):** \\( D_{hit} = (BD_{mod} + D_{elem}) \\times (1 + M_{faction}) \\)\n\n### Multi-Tier Critical Layer\n- **Final Crit Chance:** \\( CC_{final} = CC_{base} \\times (1 + M_{CC}) \\)\n- **Final Crit Multiplier:** \\( CM_{final} = CM_{base} \\times (1 + M_{CM}) \\)\n- **Expected Crit Multiplier (\\(E[CM]\\)):** \n  We will use the exact tier-overflow pseudocode provided to handle orange/red crits:\n  ```ts\n  const critTier = Math.floor(CC_final);\n  const frac = CC_final - critTier;\n  const cmTier = (tier: number) => 1 + tier * (CM_final - 1);\n  const expectedCM = (1 - frac) * cmTier(critTier) + frac * cmTier(critTier + 1);\n  ```\n\n### Projectile and DPS Layer\n- **Average Projectile Damage:** \\( D_{proj,\\,avg} = D_{hit} \\times E[CM] \\)\n- **Burst DPS:** \\( DPS_{burst} = D_{proj,\\,avg} \\times FR_{final} \\times MS_{final} \\)\n
Version 9: # Advanced Math Engine Architecture Plan\n\nBased on your architectural feedback, the math engine will be structured as a decoupled, condition-aware vector system. This ensures it can handle advanced Warframe mechanics (status weighting, conditional buffs, and beam weapons) seamlessly in the future.\n\n## User Review Required\n\n> [!NOTE]\n> Thank you for the incredible architectural recommendations! I have completely restructured the engine to use **Damage Vectors** instead of scalars, implemented the **Condition System**, and decoupled the math into a dedicated **Formulas** directory for perfect unit testing.\n>\n> Please review this finalized architecture below. If you give the green light, we will move out of Planning Mode and start writing the code!\n\n## 1. The Core Data Structures\n\n### The Condition System\nTo future-proof for Galvanized mods, Arcane buffs, and Incarnons, parsed modifiers will not be simple key-value pairs. They will be structured to include conditions:\n```ts\n{\n  stat: \"CRIT_CHANCE\",\n  value: 1.35,\n  condition: \"ON_HEADSHOT\" | \"ALWAYS\" | \"ON_KILL\" // V1 will only process \"ALWAYS\"\n}\n```\n\n### The Damage Vector\nInstead of calculating a single `hitDamage` scalar, the engine will maintain a strict Damage Vector structure at all times, making armor calculation and status weighting possible later:\n```ts\ntype DamageVector = {\n  impact: number; puncture: number; slash: number;\n  heat: number; cold: number; toxin: number; electricity: number;\n  viral: number; corrosive: number; radiation: number;\n  gas: number; magnetic: number; blast: number;\n}\n```\n\n## 2. Decoupled Engine Architecture\n\nThe engine will reside in `src/lib/engine/` and execute as a modular, strictly functional pipeline.\n\n### `engine/parser/`\nTranslates raw mod text (`\"+165% Damage\"`) into the new conditional structures.\n\n### `engine/aggregator/`\nSums all parsed modifiers across 8 mod slots into an aggregated dictionary, keeping different conditions separated.\n\n### `engine/elemental/`\nCombi
Version 10: # Finalized Math Engine Architecture Plan\n\nThis architecture represents a professional, highly decoupled, and perfectly typed Warframe math engine. It handles fractional critical tiers, damage vectors, active contexts, and isolates DPS evaluation from pure stat calculation.\n\n## User Review Required\n\n> [!NOTE]\n> These final additions are bulletproof. I have added `void`/`trueDamage` to the vector, implemented the `ConditionType` Enums and `CalculationContext`, split out the intermediate `CalculatedWeapon` structure, and added `statusChance`. \n>\n> If this architecture looks perfect to you, approve this plan and I will immediately begin execution by writing the code!\n\n## 1. The Core Data Structures (TypeScript Types)\n\n### The Damage Vector\n```ts\nexport type DamageVector = {\n  impact: number; puncture: number; slash: number;\n  heat: number; cold: number; toxin: number; electricity: number;\n  viral: number; corrosive: number; radiation: number;\n  gas: number; magnetic: number; blast: number;\n  void: number; trueDamage: number;\n}\n```\n\n### Weapon Categorization\n```ts\nexport type WeaponCategory = \"RIFLE\" | \"SHOTGUN\" | \"PISTOL\" | \"MELEE\" | \"BEAM\" | \"BOW\" | \"LAUNCHER\";\n```\n\n### The Condition System\n```ts\nexport enum ConditionType {\n  ALWAYS = \"ALWAYS\",\n  ON_HEADSHOT = \"ON_HEADSHOT\",\n  ON_KILL = \"ON_KILL\",\n  ON_STATUS = \"ON_STATUS\",\n  ON_RELOAD = \"ON_RELOAD\",\n  ON_AIM = \"ON_AIM\"\n}\n\nexport type CalculationContext = {\n  activeConditions: Set<ConditionType>;\n}\n```\n\n### The Intermediate `CalculatedWeapon`\nSeparating the pure calculation of final stats from the evaluation of DPS.\n```ts\nexport type CalculatedWeapon = {\n  damageVector: DamageVector;\n  critChance: number;\n  critMultiplier: number;\n  statusChance: number; // Final status chance\n  fireRate: number;\n  multishot: number;\n  magazineSize: number;\n  reloadTime: number;\n}\n```\n\n## 2. Decoupled Engine Architecture (`src/lib/engine/`)\n\n### `engine/parser/`\nTranslates raw mod text (`\"+16
Version 11: # Elemental Combiner Implementation Plan\n\nThe `ElementalCombiner` is one of the most critical and complex mechanics in Warframe. It must accurately reflect how elements combine into secondary damage types based strictly on Mod Slot Order and Innate Weapon Elements.\n\n## User Review Required\n\n> [!IMPORTANT]\n> The elemental combining logic requires strict adherence to Warframe's mod ordering system. I have mapped out the logic below, including the edge case of **Innate Weapon Elements**. \n>\n> Please review the pipeline below. Specifically, confirm that the logic for handling Innate Elements (combining *last* unless pulled forward by a matching mod) is correct!\n\n## 1. The Core Combining Matrix\nThe engine will use a strict lookup table for combining the 4 primary elements:\n\n- **Heat + Cold** = Blast\n- **Heat + Toxin** = Gas\n- **Heat + Electricity** = Radiation\n- **Cold + Toxin** = Viral\n- **Cold + Electricity** = Magnetic\n- **Toxin + Electricity** = Corrosive\n\n## 2. Input Data Structure\nThe combiner will need two inputs to evaluate correctly:\n1. **The Base Damage Vector:** The innate stats of the weapon (e.g., an Ignis has 100% innate Heat).\n2. **The Ordered Mod Array:** A strictly ordered array `[Slot1, Slot2, ..., Slot8]` containing parsed elemental modifiers.\n\n## 3. The Combining Pipeline\n\nThe `ElementalCombiner.ts` will execute the following pipeline:\n\n### Step 1: Element Discovery (Left-to-Right)\nWe iterate through the 8 mod slots from left to right. \n- When a primary element is found, it is placed in a \"waiting\" state.\n- When a second primary element is found, it is instantly combined with the waiting element into a secondary element.\n- This new secondary element is locked in and cannot combine again.\n\n### Step 2: Innate Element Merging\nWe check the weapon's innate `DamageVector` for primary elements (e.g., innate Toxin).\n- **If the player slotted a Toxin mod:** The innate Toxin damage is added to the *mod's position* and combined exactly when the mod combines.\n- **If th
Version 12: # Elemental Combiner Implementation Plan\n\nBased on architectural feedback, the `ElementalCombiner` will use a **Single Ordered Element Stream** approach. This avoids brittle \"special case\" rules and perfectly replicates Warframe's complex elemental merging behavior.\n\n## User Review Required\n\n> [!NOTE]\n> I have completely redesigned the pipeline! It now distinguishes between Innate Primary and Innate Secondary damage, builds a single unified stream, and merges matching innate elements directly into the mod slots rather than relying on a separate \"pull forward\" pass.\n>\n> Please review the pipeline and the expanded 7 Verification Cases below. If this architecture looks flawless, approve it and I will begin execution!\n\n## 1. Distinguishing Innate Elements\n\nBefore combining, the engine will strictly separate a weapon's innate base damage into two categories:\n- **Innate Primary Elements:** Heat, Cold, Toxin, Electric (These participate in the combining algorithm).\n- **Innate Secondary Elements:** Blast, Corrosive, Gas, Magnetic, Radiation, Viral (These **do not** combine. They sit idly and just have more damage added to them if the player mods for them).\n\n## 2. The Stream Pipeline Architecture\n\nThe `ElementalCombiner.ts` will execute the following unified pipeline:\n\n### Step 1: Build the Mod Stream\nExtract the primary elemental types from the 8 mod slots, creating an ordered array: `[Slot1, Slot2, ..., Slot8]`.\n\n### Step 2: Append Innate Primary Elements\nTake any Innate Primary Elements from the weapon and append them to the very end of the stream (essentially acting as Slot 9, Slot 10, etc.).\n\n### Step 3: Merge Matching Elements\nIterate through the stream. If an Innate Primary Element matches an element added by a mod earlier in the stream, the innate damage is merged directly into that mod's pool, and the innate element is removed from the end of the stream.\n\n### Step 4: Combine Left-to-Right\nIterate through the final unified stream from left to right:\n- When a primary element is
Version 13: # DamageVector Operations Implementation Plan\n\nBased on your request, I have reverted all the Master Calculator and Aggregator code. We will proceed strictly step-by-step. \n\nThe next foundational module to build is the **DamageVector Operations** layer. Because Warframe math relies entirely on manipulating 15-dimensional damage structures (Impact, Puncture, Slash, Heat, etc.), treating them as vectors rather than loose variables ensures safety and prevents code duplication in the future modules (Aggregator, Combiner, Faction Math).\n\n## User Review Required\n\n> [!NOTE]\n> Please review the proposed vector operations below. If these match the exact utility functions you envision using throughout the rest of the engine, approve this plan and I will build them!\n\n## The `vector.ts` Module\n\nThis module will reside in `src/lib/engine/formulas/vector.ts` (or `src/lib/engine/utils/vector.ts`) and contain pure, immutable mathematical operations.\n\n### 1. `getEmptyVector()`\nReturns a completely zeroed-out `DamageVector`. Essential for initializing baseline aggregations.\n\n### 2. `addVector(v1: DamageVector, v2: DamageVector)`\nTakes two damage vectors and returns a new vector where every corresponding damage type is summed. \n- **Use Case:** Adding Innate Damage to Modded Damage.\n\n### 3. `scaleVector(v: DamageVector, scalar: number)`\nMultiplies every property in the vector by a constant scalar.\n- **Use Case:** Applying the `+1.65` Base Damage (Serration) multiplier across all innate physical stats, or applying a Global Faction Multiplier.\n\n### 4. `sumVector(v: DamageVector)`\nReturns a single scalar representing the total numerical sum of all 15 damage types within the vector.\n- **Use Case:** Calculating the total raw hit damage to feed into the DPS formula, or computing the total innate damage pool to scale elemental mods.\n\n### 5. `mergeVector(base: DamageVector, overrides: Partial<DamageVector>)`\nCreates a new vector starting from `base`, but applies any specific keys found in `overrides`.\n- **Us
Version 14: # Regex Mod Parser Implementation Plan\n\nThe `ModParser` is the \"front door\" of the math engine. It is responsible for translating raw text from the SQLite database (e.g., `\"+165% Damage\"`) into strictly typed `Modifier` objects that the Master Orchestrator can calculate.\n\n## User Review Required\n\n> [!IMPORTANT]\n> Parsing natural language is inherently tricky. The proposed pipeline relies on a strict Regex Mapping table. \n> \n> Please review the **Stat Mapping** section closely. Did I miss any major keyword patterns that Warframe uses for mods?\n\n## 1. The Output Target\n\nThe parser must output an array of `Modifier` objects for each mod it processes. A single mod might have multiple effects (e.g., Malignant Force provides both Toxin and Status Chance), meaning one mod string maps to an *array* of `Modifier` objects.\n\n```typescript\ntype Modifier = {\n  stat: string; // e.g. \"BASE_DAMAGE\", \"toxin\", \"CRIT_CHANCE\"\n  value: number; // e.g. 1.65 (for +165%)\n  condition: ConditionType;\n  sourceSlot: number;\n};\n```\n\n## 2. The Parsing Pipeline\n\nThe parser will live in `src/lib/engine/parser/parser.ts` and follow a strict 4-step pipeline:\n\n### Step 1: Tokenization\nMods often contain multiple stats separated by newlines or commas. The parser will split the raw string into an array of individual stat lines.\n- **Input:** `\"+60% Toxin\\n+60% Status Chance\"`\n- **Tokens:** `[\"+60% Toxin\", \"+60% Status Chance\"]`\n\n### Step 2: Condition Extraction\nThe parser will check the beginning of the string for specific condition triggers using Regex, and strip the condition text from the string.\n- `\"On Headshot:\"` ➡️ `ConditionType.ON_HEADSHOT`\n- `\"On Kill:\"` ➡️ `ConditionType.ON_KILL`\n- No trigger found ➡️ `ConditionType.ALWAYS`\n\n### Step 3: Value & Sign Extraction\nThe parser will extract the number, the sign (`+` or `-`), and whether a `%` sign is present.\n- If `%` is present: Divide the value by 100 (e.g., `165` ➡️ `1.65`).\n- If no `%` is present: Keep the flat val
Version 15: # Revised Regex Mod Parser Implementation Plan\n\nBased on your structural insights, the Mod Parser will be incredibly fault-tolerant. Instead of hardcoding conditionals, it will use a centralized **Regex Registry** mapped to strict TypeScript Enums. It will also gracefully fail on unknown lines rather than crashing the database import.\n\n## User Review Required\n\n> [!NOTE]\n> I have implemented all of your architectural recommendations: the `ModifierStat` Enum, the mapping registry, the handling of handling/companion stats, and the non-crashing `ParseResult` object.\n> \n> Review this finalized design. If it looks flawless, approve it and we'll begin the build!\n\n## 1. Strict Data Structures\n\nWe will replace the string-based stats with a bulletproof Enum to completely prevent typo-related bugs.\n\n```typescript\nexport enum ModifierStat {\n  BASE_DAMAGE = \"BASE_DAMAGE\",\n  FACTION_DAMAGE = \"FACTION_DAMAGE\",\n  CRIT_CHANCE = \"CRIT_CHANCE\",\n  CRIT_MULTIPLIER = \"CRIT_MULTIPLIER\",\n  STATUS_CHANCE = \"STATUS_CHANCE\",\n  STATUS_DURATION = \"STATUS_DURATION\",\n  FIRE_RATE = \"FIRE_RATE\",\n  MULTISHOT = \"MULTISHOT\",\n  MAGAZINE_CAPACITY = \"MAGAZINE_CAPACITY\",\n  RELOAD_SPEED = \"RELOAD_SPEED\",\n  AMMO_MAX = \"AMMO_MAX\",\n  PUNCH_THROUGH = \"PUNCH_THROUGH\",\n  PROJECTILE_SPEED = \"PROJECTILE_SPEED\",\n  ZOOM = \"ZOOM\",\n  RECOIL = \"RECOIL\",\n  ACCURACY = \"ACCURACY\",\n  SPREAD = \"SPREAD\",\n  // Companions\n  LINK_HEALTH = \"LINK_HEALTH\",\n  LINK_ARMOR = \"LINK_ARMOR\",\n  LINK_SHIELDS = \"LINK_SHIELDS\",\n}\n// Note: Elemental / IPS damage types will continue to map directly to the `DamageType` union we built.\n\nexport type ParseResult = {\n  modifiers: Modifier[];\n  unknownLines: string[]; // Logs unparseable lines (e.g., \"+5 Health/s\") without crashing\n};\n```\n\n## 2. The Regex Registry Architecture\n\nInstead of hundreds of `if` statements, the parser will iterate through a cleanly maintained registry object:\n\n```typescript\ntype StatMapping = {\n  regex: RegExp;\n  stat: Modif
Version 16: # Revised Regex Mod Parser Implementation Plan\n\nThe `ModParser` is now architected as a two-stage decoupled pipeline (Text Extraction ➡️ Stat Interpretation) with faction-specific Enums and a massive Database Coverage Test.\n\n## User Review Required\n\n> [!NOTE]\n> I have incorporated the Faction-specific Enums, the future-proofed `ConditionType` Enums, and the Text Extraction layer. Most importantly, I've added a **Database Coverage Test** to ensure the registry can handle all 1,800 mods.\n> \n> Review this final parser architecture below. If approved, we execute!\n\n## 1. Strict Data Structures\n\nWe will break `FACTION_DAMAGE` out into specific enemy types to prevent overlapping multipliers, and add future-proof `ConditionType` enums.\n\n```typescript\nexport enum ConditionType {\n  ALWAYS = \"ALWAYS\",\n  ON_HEADSHOT = \"ON_HEADSHOT\",\n  ON_KILL = \"ON_KILL\",\n  ON_AIM = \"ON_AIM\",\n  ON_RELOAD = \"ON_RELOAD\",\n  ON_STATUS = \"ON_STATUS\",\n  ON_CRIT = \"ON_CRIT\",\n  ON_WEAPON_SWAP = \"ON_WEAPON_SWAP\",\n}\n\nexport enum ModifierStat {\n  BASE_DAMAGE = \"BASE_DAMAGE\",\n  FACTION_DAMAGE_GRINEER = \"FACTION_DAMAGE_GRINEER\",\n  FACTION_DAMAGE_CORPUS = \"FACTION_DAMAGE_CORPUS\",\n  FACTION_DAMAGE_INFESTED = \"FACTION_DAMAGE_INFESTED\",\n  FACTION_DAMAGE_CORRUPTED = \"FACTION_DAMAGE_CORRUPTED\",\n  FACTION_DAMAGE_MURMUR = \"FACTION_DAMAGE_MURMUR\",\n  CRIT_CHANCE = \"CRIT_CHANCE\",\n  CRIT_MULTIPLIER = \"CRIT_MULTIPLIER\",\n  STATUS_CHANCE = \"STATUS_CHANCE\",\n  STATUS_DURATION = \"STATUS_DURATION\",\n  FIRE_RATE = \"FIRE_RATE\",\n  MULTISHOT = \"MULTISHOT\",\n  MAGAZINE_CAPACITY = \"MAGAZINE_CAPACITY\",\n  RELOAD_SPEED = \"RELOAD_SPEED\",\n  AMMO_MAX = \"AMMO_MAX\",\n  PUNCH_THROUGH = \"PUNCH_THROUGH\",\n  PROJECTILE_SPEED = \"PROJECTILE_SPEED\",\n  ZOOM = \"ZOOM\",\n  RECOIL = \"RECOIL\",\n  ACCURACY = \"ACCURACY\",\n  SPREAD = \"SPREAD\",\n  LINK_HEALTH = \"LINK_HEALTH\",\n  LINK_ARMOR = \"LINK_ARMOR\",\n  LINK_SHIELDS = \"LINK_SHIELDS\",\n}\n\nexport type ParseResult = {\n  modifiers: Modifier[
Version 17: # STAT_REGISTRY Expansion Implementation Plan\n\nTo drive our parser coverage towards 100%, we will systematically map the highest-frequency missing stats exposed by our coverage report. This requires adding new TypeScript Enums and flexible regex patterns.\n\n## User Review Required\n\n> [!IMPORTANT]\n> The primary design question here is **strictness vs. aggregation**. \n> For example: Should `\"Melee Damage\"` map to a new `ModifierStat.MELEE_DAMAGE`, or should it just map to `ModifierStat.BASE_DAMAGE`?\n> \n> **My Proposal:** Map strictly to `MELEE_DAMAGE`. The aggregator will then intelligently sum `MELEE_DAMAGE` into the `baseDamage` pool *only* if the current `WeaponCategory === \"MELEE\"`. This prevents hilarious bugs where equipping a Melee Mod onto a Rifle accidentally scales its damage.\n> \n> Please confirm if you agree with this strict mapping approach!\n\n## 1. Expanding `ModifierStat` (types.ts)\n\nWe will expand the Enum to include all the missing Warframe and Melee categories:\n\n### Melee Stats\n- `MELEE_DAMAGE`\n- `ATTACK_SPEED` (often distinct from `FIRE_RATE`)\n- `HEAVY_ATTACK_EFFICIENCY`\n- `MELEE_COMBO_DURATION`\n- `MELEE_COMBO_COUNT_CHANCE`\n\n### Warframe Stats\n- `HEALTH`\n- `SHIELD_CAPACITY`\n- `ARMOR`\n- `ABILITY_STRENGTH`\n- `ABILITY_DURATION`\n- `ABILITY_RANGE`\n- `ABILITY_EFFICIENCY`\n- `ENERGY_MAX`\n- `SPRINT_SPEED`\n- `MOBILITY`\n- `PARKOUR_VELOCITY`\n- `AIM_GLIDE_WALL_LATCH`\n- `SHIELD_RECHARGE`\n- `SHIELD_RECHARGE_DELAY`\n\n## 2. Flexible Regex Mapping (registry.ts)\n\nMany stats failed because of trailing parentheticals, such as `\"Fire Rate (x2 for Bows)\"` or `\"Critical Chance (x2 for Heavy Attacks)\"`. \nWe will use loose regex matching to ensure we capture the core stat without getting tripped up by flavor text.\n\n```typescript\nexport const STAT_REGISTRY: StatMapping[] = [\n  // ... existing mappings\n  \n  // Melee\n  { regex: /^Melee Damage/i, stat: ModifierStat.MELEE_DAMAGE },\n  { regex: /^Attack Speed/i, stat: ModifierStat.ATTACK_SPEED },\n  \n  // Regex loosening
Version 18: # Global Optimizer Parser Architecture Plan\n\nBased on your structural insights, we are shifting the Parser's goal from \"Weapon Calculator\" to **\"Global Loadout Optimizer\"**. The parser will act purely as a strict data extractor, preserving the precise meaning of every mod (e.g., `MELEE_DAMAGE` vs `BASE_DAMAGE`) and tagging them with `ModifierCategory` scopes. The Aggregator and Evaluator will be responsible for resolving how these modifiers interact across Warframes, Weapons, and Companions.\n\n## User Review Required\n\n> [!NOTE]\n> I have incorporated the `ModifierCategory` Enum, the `scope` field, the massive missing \"Concept\" expansions (Auras, Exilus, Abilities, Companions), and our new ambitious goal of **95% database coverage**.\n> \n> Review this finalized optimizer architecture. If approved, we execute!\n\n## 1. Defining Scope (types.ts)\n\nTo allow the optimizer to query and filter mods across entire loadouts, we will introduce `ModifierCategory`. Every mapping in the `STAT_REGISTRY` will explicitly define what scopes it can apply to.\n\n```typescript\nexport enum ModifierCategory {\n  WEAPON = \"WEAPON\",\n  PRIMARY = \"PRIMARY\",\n  SECONDARY = \"SECONDARY\",\n  MELEE = \"MELEE\",\n  WARFRAME = \"WARFRAME\",\n  COMPANION = \"COMPANION\",\n  ARCHWING = \"ARCHWING\",\n  OPERATOR = \"OPERATOR\",\n  UNIVERSAL = \"UNIVERSAL\"\n}\n\nexport type Modifier = {\n  stat: ModifierStat | DamageType;\n  value: number;\n  condition: ConditionType;\n  sourceSlot: number;\n  scope: ModifierCategory[]; // Explicit targeting (e.g., [PRIMARY, SECONDARY])\n};\n```\n\n## 2. Concept-Based Expansion (types.ts & registry.ts)\n\nInstead of adding top missing lines one-by-one, we will systematically add entire missing **Concepts** to the `ModifierStat` Enum and map them in the registry:\n\n### Concept: Warframe Survivability\n- `HEALTH`, `SHIELD_CAPACITY`, `ARMOR`, `SHIELD_RECHARGE`, `SHIELD_RECHARGE_DELAY`\n\n### Concept: Warframe Abilities\n- `ABILITY_STRENGTH`, `ABILITY_DURATION`, `ABILITY_RANGE`, `ABILITY_EFFICIENC
Version 19: # Final Optimizer Parser Architecture Plan\n\nThe final iteration of the Mod Parser officially evolves it into a robust **Loadout Optimizer Engine**. We are solving two monumental optimization problems: Cross-Scope Buffing (e.g., Auras) and Mechanical Effects (e.g., Hunter Munitions).\n\n## User Review Required\n\n> [!NOTE]\n> I have completely refactored the data structures to separate `equipScope` and `targetScope`. I have also introduced `ModifierKind` to catch unique mechanics as `EFFECT` rather than dropping them.\n> \n> Review this finalized robust architecture. If approved, we execute and begin clearing out the registry!\n\n## 1. Advanced Scope Mechanics (types.ts)\n\nAuras and Exilus mods prove that what a mod is equipped *on* is rarely what it *affects*. We will decouple the scopes.\n\n```typescript\nexport enum ModifierCategory {\n  WEAPON = \"WEAPON\",\n  PRIMARY = \"PRIMARY\",\n  SECONDARY = \"SECONDARY\",\n  MELEE = \"MELEE\",\n  WARFRAME = \"WARFRAME\",\n  COMPANION = \"COMPANION\",\n  ENEMY = \"ENEMY\",\n  UNIVERSAL = \"UNIVERSAL\"\n}\n\nexport enum ModifierKind {\n  STAT = \"STAT\",\n  EFFECT = \"EFFECT\",\n  UNKNOWN = \"UNKNOWN\"\n}\n```\n\n## 2. The Universal Modifier Type\n\nThe core `Modifier` interface now elegantly supports both strict numerical stats and complex abstract mechanics.\n\n```typescript\nexport type Modifier = {\n  kind: ModifierKind;\n  \n  // Populated if kind === STAT\n  stat?: ModifierStat | DamageType;\n  value?: number;\n\n  // Populated if kind === EFFECT\n  effectId?: string; // e.g. \"HUNTER_MUNITIONS\", \"ACID_SHELLS\"\n\n  condition: ConditionType;\n  sourceSlot: number;\n  \n  equipScope: ModifierCategory[]; // e.g. [WARFRAME]\n  targetScope: ModifierCategory[]; // e.g. [MELEE]\n};\n```\n\n## 3. The Multi-Kind Registry\n\nThe `STAT_REGISTRY` will be upgraded to support classifying `EFFECT` mechanics alongside pure `STAT` entries. This guarantees that complex descriptions are parsed and classified, rather than failing.\n\n```typescript\ntype StatMapping = {\n  regex:
Version 20: # Coverage Classification Expansion Plan\n\nThis plan outlines the final push to drive our classification metric from 41% towards 95%, methodically grouping the remaining unparsed lines into four actionable categories.\n\n## User Review Required\n\n> [!TIP]\n> **Regarding Group B (Broken Extraction):**\n> You noted that lines like `\"Converts Secondary ammo pickups to 92% of Ammo Pick Up\"` are being stripped of their `92%`, leaving behind `\"Converts Secondary ammo pickups to of Ammo Pick Up\"`.\n> \n> Interestingly, this is actually our extraction pipeline working *exactly as intended*! It successfully extracted `0.92` into the `value` property of the `Modifier`, leaving the text behind. This means we have successfully parameterized the mechanic! \n> \n> We simply need to map the leftover \"garbage\" text to the correct Enum. I've detailed this approach below. Let me know if you agree!\n\n## Group A: Missing Simple Stats (~20-30%)\nWe will expand `ModifierStat` and the `MODIFIER_REGISTRY` to catch these straightforward numerical scalars.\n\n**New Enums:**\n- `STATUS_DAMAGE`, `FINISHER_DAMAGE`, `RANGE`, `BLAST_RANGE`, `TOXIN_RESISTANCE`, `AIMING_MOVEMENT_SPEED`, `HEAVY_ATTACK_WIND_UP`, `KNOCKDOWN_RESISTANCE`, `KNOCKDOWN_RECOVERY`.\n\n## Group B: Embedded Parameters (~10-15%)\nBecause our pipeline extracts numbers first, it elegantly separates parameters from their mechanical descriptions. \n\nFor example, `\"20% of Damage converted into Impact\"` becomes:\n- `value: 0.20`\n- `text: \"of Damage converted into Impact\"`\n\nWe will map these \"leftover\" strings to `STAT` or `EFFECT` buckets so they are classified correctly, retaining the numerical `value` for future evaluator logic.\n\n**New Enums:**\n- `DAMAGE_CONVERSION_IMPACT` (STAT)\n- `DAMAGE_CONVERSION_PUNCTURE` (STAT)\n- `DAMAGE_CONVERSION_SLASH` (STAT)\n- `AMMO_MUTATION` (EFFECT)\n- `HEALTH_ORB_CONVERSION` (EFFECT)\n- `COMBO_DURATION` (STAT)\n\n## Group C: Syndicate Effects\nWe will introduce `EFFECT` identifiers for the 6 syndicate procs.\n\n**New Effect
Version 21: # Effect-Driven Classification Expansion Plan\n\nThis plan outlines our strategy to classify the remaining unparsed lines, shifting focus towards abstract mechanics (`EFFECT`) and structural payload metadata, guided by a new Root Pattern analysis tool.\n\n## User Review Required\n\n> [!TIP]\n> I have incorporated your insights regarding `EffectMetadata` payloads for mechanics like `DAMAGE_CONVERSION`! \n> I also added your requested **\"Root Pattern Frequency Report\"** as the very first execution step. We will build an analyzer that groups similar phrases before blindly adding registry strings.\n> \n> Review this finalized plan. If approved, we will build the Root Pattern analyzer first!\n\n## 1. Advanced Effect Metadata (types.ts)\nWe will expand the `Modifier` interface to support an optional `metadata` payload. This allows complex mechanics like Damage Conversion and Ammo Mutation to store their specific ratios or targets.\n\n```typescript\nexport type EffectMetadata = {\n  targetDamageType?: DamageType;\n  ratio?: number;\n  [key: string]: any;\n};\n\nexport type Modifier = {\n  kind: ModifierKind;\n  \n  stat?: ModifierStat | DamageType;\n  value?: number;\n\n  effectId?: string;\n  metadata?: EffectMetadata; // Structured payload for the Evaluator\n\n  condition: ConditionType;\n  sourceSlot: number;\n  equipScope: ModifierCategory[];\n  targetScope: ModifierCategory[];\n};\n```\n\n## 2. Updated Classification Groups\n\n### Group A: Missing Simple Stats\nStraightforward numerical scalars mapped as `ModifierKind.STAT`.\n- `STATUS_DAMAGE`, `FINISHER_DAMAGE`, `RANGE`, `BLAST_RANGE`, `TOXIN_RESISTANCE`, `AIMING_MOVEMENT_SPEED`, `HEAVY_ATTACK_WIND_UP`, `KNOCKDOWN_RESISTANCE`, `KNOCKDOWN_RECOVERY`.\n\n### Group B: Parameterized Effects\nPreviously treated as stats, these are mechanics (`ModifierKind.EFFECT`) that utilize the `metadata` payload.\n\n**Examples:**\n- `DAMAGE_CONVERSION` (`metadata: { targetDamageType: \"impact\" }`)\n- `AMMO_MUTATION` (`metadata: { ratio: 0.92 }`)\n\n### Group C: Syndicate Effects
Version 22: # Advanced Parameterized Mechanics Implementation Plan\n\nFollowing the insights from the Root Pattern Frequency Report, we are transitioning from simple STAT mappings into modeling parameterized game mechanics and states.\n\n## User Review Required\n> [!NOTE]\n> Regarding `Damage to <FACTION>` still showing up: I investigated this and found that the parser is working perfectly for Grineer/Corpus/Infested! The remaining 8 lines were specifically `Damage to Orokin` and `Damage to Sentients`, which simply weren't in the registry yet. They will be added.\n\n> [!TIP]\n> I completely agree with your assessment that we need to start modeling game systems rather than parsing text. I have outlined the architectural changes below to rename `metadata` to `parameters` and introduce the advanced parameterized effect families you suggested.\n\n## 1. Architectural Upgrades (types.ts)\n### Adding `parameters` to `Modifier`\nWe will rename the existing `metadata` field to `parameters` and use `Record<string, unknown>` to formally represent structured payloads for `ModifierKind.EFFECT`.\n\n### Adding `SQUAD` Scope\nWe will introduce `ModifierCategory.SQUAD` to properly model target scopes that affect the entire squad (e.g., Auras, passive team buffs).\n\n## 2. Second-Pass Condition Extraction (parser.ts)\nCurrently, `parser.ts` only looks for prefix conditions like `\"On Headshot: +120% Crit Chance\"`. \nWe will introduce a **suffix condition pass** to catch things like `\"Critical Chance when Aiming\"`.\n- It will strip `when Aiming` from the line and assign `ConditionType.ON_AIM` to the modifier.\n- This gracefully turns \"Critical Chance when Aiming\" into standard `CRIT_CHANCE` with the `ON_AIM` condition, avoiding an explosion of new enums.\n\n## 3. Parameterized Effect Families (registry.ts)\nInstead of hardcoding a dozen individual `EFFECT` or `STAT` ids, we will model the top root patterns as flexible parameterized families.\n*(Note: Because our `STAT_REGISTRY` operates via static objects, we will write out the regex ent
Version 23: # Advanced System Modeling & Type Safety Plan\n\nFollowing your architectural review, this plan moves away from loosely typed records and monolithic parsing, introducing strict discriminated unions, decoupled subsystems, and highly specific game-logic effect families.\n\n## 1. Strictly-Typed Effect Parameters (types.ts)\nInstead of a generic `Record<string, unknown>`, we will implement a discriminated union `EffectParameters`. This ensures absolute type safety for the Evaluator when constructing mechanics.\n\n```typescript\nexport type ResistanceParameters = { damageType: DamageType };\nexport type StackingBuffParameters = {\n  trigger: ConditionType;\n  stat: ModifierStat;\n  value: number;\n  duration: number;\n  maxStacks: number;\n};\nexport type ElementConversionParameters = { target: DamageType; value: number }; // Partial (e.g. 20% to Slash)\nexport type ElementReplacementParameters = { source: \"ALL_PHYSICAL\" | \"ALL_ELEMENTAL\"; target: DamageType }; // Full (e.g. All Physical to Heat)\nexport type AmmoMutationParameters = { ratio: number };\n\nexport type EffectParameters =\n  | ResistanceParameters\n  | StackingBuffParameters\n  | ElementConversionParameters\n  | ElementReplacementParameters\n  | AmmoMutationParameters;\n```\n*`Modifier` will then use `parameters?: EffectParameters`.*\n\n## 2. Condition Extractor Subsystem\nWe will extract the condition parsing (both prefixes like \"On Headshot:\" and suffixes like \"when Aiming\") out of `parser.ts` into a dedicated `ConditionExtractor.ts` module.\n\nThis subsystem will normalize:\n- `while aiming` -> `ConditionType.ON_AIM`\n- `on kill` -> `ConditionType.ON_KILL`\n- `after reload` -> `ConditionType.ON_RELOAD`\n\nThis dramatically simplifies `parser.ts` and allows conditions to grow independently.\n\n## 3. Structural Stat & Scope Changes\n- **ModifierStat**: Replace `ELEMENTAL_RESISTANCE` with a generic `RESISTANCE`.\n  - Example: `stat: ModifierStat.RESISTANCE`, `parameters: { damageType: \"toxin\" }`\n- **ModifierCategory**: Add `SELF` to explicitl
Version 24: # Phase 1: Effect Foundation\n\nFollowing your guidance, we will map the structural foundation for the game's most complex mechanics: Hunter Munitions, Adaptation, Growing Power, and the Galvanized/Arcane stacking family.\n\n## User Review Required\n> [!IMPORTANT]\n> **Dynamic Regex Capturing in Registry**: To cleanly support the Galvanized/Arcane stacking mechanics (e.g. `+30% Damage for 4s. Stacks up to 12x`), I propose allowing `STAT_REGISTRY` to use dynamic functions instead of static objects for `parameters`. This will let us capture `Damage` (match 1), `4` (match 2), and `12` (match 3) from a single Regex, assigning them directly to `StackingBuffParameters` without hardcoding every single stacking mod! Let me know if you approve this approach!\n\n> [!NOTE]\n> **Hunter Munitions Tag Stripping**: The actual text in the DB for Hunter Munitions is `+30% chance to apply <DT_SLASH_COLOR> on Critical`. Currently, our parser blindly strips `<...>` tags, destroying the word `Slash`. I will introduce a pre-parsing cleanup phase that replaces specific tags (like `<DT_SLASH_COLOR>`) with their semantic word (`Slash`) before passing it to the parser.\n\n## Proposed Implementation\n\n### 1. Condition Extractor Expansion\nWe will add the missing endgame triggers to `ConditionExtractor.ts`:\n- `When Damaged:` -> `ConditionType.ON_DAMAGE_TAKEN`\n- `On Status Effect with Weapon:` -> `ConditionType.ON_STATUS_WEAPON`\n\n### 2. The Galvanized & Arcane Engine (Dynamic Registry)\nInstead of hardcoding `Galvanized Chamber` and `Primary Merciless`, we will introduce dynamic mapping to the `STAT_REGISTRY`:\n```typescript\n{\n  regex: /(.*) for (\\d+)s\\. Stacks up to (\\d+)x/i,\n  kind: ModifierKind.EFFECT,\n  effectId: \"STACKING_BUFF\",\n  buildParameters: (match) => ({\n    stat: mapStringToStat(match[1]), // Maps \"Damage\" -> BASE_DAMAGE\n    duration: parseInt(match[2]),\n    maxStacks: parseInt(match[3])\n  }),\n  // ...\n}\n```\nThis single entry will flawlessly parse *all* Galvanized mods, Dexterity/Merciless/Deadhead Arca
Version 25: # Phase 1: Effect Foundation (Revised)\n\nFollowing your architectural feedback, we will formally separate representation (Parser) from behavior (Evaluator). The parser's job is simply to classify the mod accurately and hand off parameterized mechanic data.\n\n## 1. Effect Families vs. Named Effects (`types.ts`)\nWe will split the current loosely typed `effectId: string` into two explicit enums to separate generic systemic patterns from uniquely named mods:\n\n```typescript\nexport enum EffectFamily {\n  STACKING_BUFF = \"STACKING_BUFF\",\n  AMMO_MUTATION = \"AMMO_MUTATION\",\n  DAMAGE_CONVERSION = \"DAMAGE_CONVERSION\",\n  DAMAGE_REPLACEMENT = \"DAMAGE_REPLACEMENT\",\n  SYNDICATE_PROC = \"SYNDICATE_PROC\"\n}\n\nexport enum EffectType {\n  HUNTER_MUNITIONS = \"HUNTER_MUNITIONS\",\n  ADAPTATION = \"ADAPTATION\",\n  GROWING_POWER = \"GROWING_POWER\",\n  CHANCE_TO_EXPLODE = \"CHANCE_TO_EXPLODE\"\n}\n```\n*`Modifier` will be updated to hold either `effectFamily?: EffectFamily` or `effectType?: EffectType`.*\n\n## 2. Dynamic Registry Functions (`registry.ts`)\nInstead of `buildParameters`, we will allow generic regexes to construct the entire modifier dynamically via `buildModifier`.\n\n```typescript\ntype StatMapping = {\n  regex: RegExp;\n  // Fallback static fields...\n  kind?: ModifierKind;\n  stat?: ModifierStat | DamageType;\n  // ...\n  // Dynamic Builder\n  buildModifier?: (match: RegExpMatchArray, baseCondition: ConditionType) => Partial<Modifier>;\n};\n```\nThis allows the Galvanized/Arcane pattern `/(.*) for (\\d+)s\\. Stacks up to (\\d+)x/` to build a `STACKING_BUFF` family object natively, including the base `trigger` condition parsed by `ConditionExtractor`!\n\n## 3. Tag Normalization Preprocessor (`parser.ts`)\nWe will create a `normalizeGameTags(raw: string)` pipeline inside the parser to convert presentation markup into semantic text *before* the regexes evaluate it:\n- `<DT_SLASH_COLOR>` → `Slash`\n- `<DT_HEAT_COLOR>` → `Heat`\n- `<DT_TOXIN_COLOR>` → `Toxin`\n*(This ensures \"Hunter Munitions\
Version 26: # Phase 1: Effect Foundation (Finalized)\n\nThis finalized architecture formally isolates representation from evaluation while giving the parser advanced scaling tools: registry priority, strict typing, and full traceability.\n\n## 1. Strictly Mutually Exclusive Effects (`types.ts`)\nTo prevent corrupt runtime states, `Modifier` will be split into a discriminated union mapping strictly to `STAT`, `FAMILY`, or `NAMED` types.\n\n```typescript\nexport type StatModifier = {\n  kind: ModifierKind.STAT;\n  stat: ModifierStat | DamageType;\n  value: number;\n};\n\nexport type FamilyEffectModifier = {\n  kind: ModifierKind.EFFECT;\n  effectFamily: EffectFamily;\n  parameters: EffectParameters;\n};\n\nexport type NamedEffectModifier = {\n  kind: ModifierKind.EFFECT;\n  effectType: EffectType;\n};\n\n// Base properties all modifiers share:\nexport type ModifierBase = {\n  condition: ConditionType;\n  sourceSlot: number;\n  sourceText: string; // Parser Provenance\n  equipScope: ModifierCategory[];\n  targetScope: ModifierCategory[];\n};\n\nexport type Modifier = ModifierBase & (StatModifier | FamilyEffectModifier | NamedEffectModifier);\n```\n\n## 2. Parser Provenance & Tag Preprocessing (`parser.ts`)\n### Provenance\nEvery `Modifier` will receive a `sourceText: string` field carrying the exact un-parsed line (e.g., `\"+30% Damage for 4s. Stacks up to 12x\"`). This ensures the evaluator and debugger can easily trace weird calculations back to the exact mod string.\n\n### Tag Normalization\nBefore regex extraction, we will normalize visual markup:\n- `<DT_SLASH_COLOR>` → `Slash`\n- `<DT_HEAT_COLOR>` → `Heat`\n- `<DT_TOXIN_COLOR>` → `Toxin`\n\n## 3. Dynamic Registry & Priority Sorting (`registry.ts`)\nWe will formally introduce `priority` to the registry, ensuring specific patterns run *before* generic ones.\n\n```typescript\nexport type StatMapping = {\n  regex: RegExp;\n  priority: number; // Higher numbers evaluate first (e.g., 100 > 10)\n  \n  // Static Fallback Fields\n  kind?: ModifierKind;\n  stat?: ModifierSta
Version 27: # End-to-End Effect Evaluator Architecture\n\nThe goal is to implement the `EffectRegistry` and Evaluator pipeline to bridge the gap between parsed effects (e.g., `NamedEffectModifier`) and real mathematical outcomes (DPS). We will test this end-to-end with **Hunter Munitions**.\n\n## Proposed Changes\n\n### 1. Update Aggregator\nCurrently, the Aggregator drops any modifier that is not `ModifierKind.STAT`. \n\n#### [MODIFY] [aggregator.ts](file:///c:/Users/jkiri/Documents/WF%20Calc/calculator_app/src/lib/engine/aggregator/aggregator.ts)\n- Extend `AggregatedModifiers` to include `activeEffects: (FamilyEffectModifier | NamedEffectModifier)[]`.\n- Update `aggregateModifiers` to push `ModifierKind.EFFECT` entries into `activeEffects` (if their condition is met or is `ALWAYS`).\n\n### 2. Create the Effect Registry\nThe Effect Registry serves as the bridge between \"what the mod says\" and \"what the mod actually does.\"\n\n#### [NEW] [EffectRegistry.ts](file:///c:/Users/jkiri/Documents/WF%20Calc/calculator_app/src/lib/engine/evaluator/EffectRegistry.ts)\n- Define the `EffectDefinition` interface:\n  ```typescript\n  export interface EffectContext {\n    weapon: CalculatedWeapon;\n    aggregated: AggregatedModifiers;\n    addDPS: (source: string, value: number) => void;\n    // Future: addStat(), addCondition(), etc.\n  }\n\n  export interface EffectDefinition {\n    type: EffectType | EffectFamily;\n    // Using a class or object with an evaluate method:\n    evaluate: (modifier: EffectModifier, context: EffectContext) => void;\n  }\n  ```\n- Implement the `EffectRegistry` class with `.register()` and `.get()`.\n\n### 3. Implement Hunter Munitions\n#### [NEW] [HunterMunitions.ts](file:///c:/Users/jkiri/Documents/WF%20Calc/calculator_app/src/lib/engine/evaluator/effects/HunterMunitions.ts)\n- Register `EffectType.HUNTER_MUNITIONS`.\n- In `evaluate()`:\n  - Calculate Slash Proc Tick Damage: `ModdedBaseDamage * 0.35 * (1 + FactionDamage)^2`.\n  - Calculate Total Proc Damage: `Tick Damage * 6` (over 6 seconds).\n  - Cal
Version 28: # Phase 3: Effect Archetypes & Context Expansion\n\nThe objective of this phase is to finalize the three foundational effect archetypes and introduce the `EvaluationContext`. This will prove the evaluator can handle temporary buffs (Growing Power) and stateful defensive mechanics (Adaptation), while integrating external environmental data (enemies/missions).\n\n## Proposed Architecture\n\n### 1. The Evaluation Context\nThe `CalculationContext` will be promoted to an `EvaluationContext` capable of representing the broader environment necessary for an optimizer to make comparative decisions.\n\n#### [NEW/MODIFY] `types.ts`\n```typescript\nexport type FactionType = \"GRINEER\" | \"CORPUS\" | \"INFESTED\" | \"CORRUPTED\" | \"MURMUR\" | \"OROKIN\" | \"SENTIENT\";\n\nexport type EnemyState = {\n  faction: FactionType;\n  level: number;\n  baseArmor: number;\n  currentArmor: number;\n  damageOutputVector?: DamageVector; // What damage the enemy deals to the player\n};\n\nexport type MissionState = {\n  steelPath: boolean;\n};\n\nexport type EvaluationContext = {\n  // loadout: LoadoutState; // (Placeholder for future Loadout representation)\n  enemy: EnemyState;\n  mission: MissionState;\n  activeConditions: Set<ConditionType>;\n};\n```\n*Note: `CalculationContext` in the master orchestrator will be replaced with `EvaluationContext`.*\n\n### 2. Archetype 2: Buff Effects (Growing Power)\nBuff effects produce `statChanges` rather than direct DPS contributions. \n\n#### [NEW] `GrowingPower.ts`\n- Implement `EffectType.GROWING_POWER` in the registry.\n- Evaluate Trigger: Check if `context.activeConditions.has(ConditionType.ON_STATUS_WEAPON)`.\n- Behavior: If triggered, return:\n  ```typescript\n  return {\n    contributions: [],\n    statChanges: [{\n      kind: ModifierKind.STAT,\n      stat: ModifierStat.ABILITY_STRENGTH,\n      value: 0.25,\n      condition: ConditionType.ALWAYS,\n      sourceSlot: modifier.sourceSlot,\n      sourceText: \"Growing Power Buff\",\n      equipScope: [ModifierCategory.WARFRAME],\n      targ
Version 29: # Phase 4: Build Scorer Architecture\n\nTo enable an optimizer, we need a mathematical heuristic that defines \"best\". The **Build Scorer** is responsible for taking a fully resolved calculation result (after the `EffectResolutionEngine` has finished) and collapsing it into a multi-dimensional `BuildScore`.\n\n## Proposed Architecture\n\n### 1. Define the BuildScore Interface\nWe will define the target output for the scorer. This allows the future optimizer to prioritize different weights (e.g., maximizing `survivability` for an Inaros build, or `statusDPS` for a Saryn build).\n\n#### [NEW] `src/lib/engine/optimizer/scorerTypes.ts`\n```typescript\nexport interface BuildScore {\n  directDPS: number;\n  statusDPS: number;\n  survivability: number;\n  utility: number;\n  \n  // Future: energyEconomy, mobility, etc.\n}\n\nexport interface ScoreWeights {\n  directDPS: number;\n  statusDPS: number;\n  survivability: number;\n  utility: number;\n}\n```\n\n### 2. Implement the Scorer Engine\nCreate a scoring layer that interprets the output of `resolveWeaponEffects`. \n\n#### [NEW] `src/lib/engine/optimizer/scorer.ts`\n- Implement `scoreWeaponBuild(result: MasterCalculationResult): BuildScore`.\n- **Direct DPS Calculation**: \n  - Iterate through `result.contributions`.\n  - Sum the `dps` of all contributions where `type === \"DIRECT\"`.\n- **Status DPS Calculation**: \n  - Sum the `dps` of all contributions where `type === \"STATUS\"` or `\"EFFECT\"`.\n- **Utility Calculation**:\n  - Analyze `result.calculatedWeapon` for QoL stats (e.g., `reloadTime`, `magazineSize`, `fireRate` ratios, or `Punch Through` if added).\n  - Apply a mathematical normalization so utility can be ranked against pure DPS (e.g., higher uptime -> higher utility score).\n- **Survivability Calculation**:\n  - Since this is currently a Weapon context, `survivability` will default to `0`. However, if the weapon provides healing (e.g., Furis Winds of Purity) or resistance buffs, those could theoretically contribute.\n\n### 3. Create a Weighted Evalua
Version 30: # Phase 5: Random Search Optimizer Prototype\n\nThe objective of this phase is to validate the entire architecture end-to-end by building the first working prototype of the **Auto-Optimizer**. We will build a random-search generator that takes a curated pool of mods, constructs legal loadouts, evaluates them, and ranks them by user preference.\n\n## Proposed Architecture\n\n### 1. In-Game Mod Representation & Rules\nBefore generating builds, we must formally define what a \"Mod\" is and what constraints govern a \"Build\".\n\n#### [NEW] `src/lib/engine/optimizer/optimizerTypes.ts`\n```typescript\nexport interface InGameMod {\n  id: string; // e.g., \"serration\"\n  name: string; // \"Serration\"\n  text: string; // Raw text to be fed into the parser\n  capacityCost?: number; // Ignored for now\n  polarity?: string; // Ignored for now\n}\n\nexport interface BuildRules {\n  maxSlots: number; // e.g., 8\n  maxCapacity: number; // e.g., 60\n  allowDuplicates: boolean; // e.g., false\n}\n```\n\n### 2. The Build Generator\nA module responsible for creating legal combinations of mods.\n\n#### [NEW] `src/lib/engine/optimizer/generator.ts`\n- Implement `generateRandomBuilds(pool: InGameMod[], count: number, rules: BuildRules): InGameMod[][]`.\n- It will randomly select 1 to `maxSlots` unique mods from the `pool` per build, ensuring `allowDuplicates = false`.\n- *Performance Note*: `parseMod` should be run on the `pool` *before* optimization, so the random generator operates on pre-parsed arrays of `Modifier[]` rather than repeatedly running regex operations 1,000 times.\n\n### 3. The Evaluation Pipeline Wrapper\nA single function that wraps the entire calculation flow, representing the fundamental unit of the Optimizer.\n\n#### [NEW] `src/lib/engine/optimizer/pipeline.ts`\n```typescript\nexport interface EvaluatedBuild {\n  build: InGameMod[];\n  score: number;\n  metrics: WeaponMetrics;\n  result: MasterCalculationResult;\n}\n\nexport function evaluateBuild(\n  build: InGameMod[], \n  baseStats: WeaponBaseStats, \n  con
Version 31: # Phase 6: Capacity Constraints & Benchmark Validation\n\nCurrently, our optimizer can find the highest theoretical DPS combination, but it assumes unlimited Mod Capacity. Phase 6 introduces real-world Warframe constraints (Capacity limits) and pits the Optimizer against a known community \"Meta\" build to validate evaluator accuracy.\n\n## Proposed Architecture\n\n### 1. Mod Capacity & Polarity Definitions\nWe will extend the existing types to support the concept of drain and polarity. While full slot assignment (Aura/Stance matching) will come later, we will build the foundation for it now.\n\n#### [MODIFY] `src/lib/engine/optimizer/optimizerTypes.ts`\n```typescript\nexport enum Polarity {\n  MADURAI = \"MADURAI\",\n  VAZARIN = \"VAZARIN\",\n  NARAMON = \"NARAMON\",\n  ZENURIK = \"ZENURIK\",\n  PENJAGA = \"PENJAGA\",\n  UMBRA = \"UMBRA\"\n}\n\n// InGameMod will now require `capacityCost: number` and optional `polarity: Polarity`.\n\nexport interface BuildRules {\n  maxSlots: number;\n  maxCapacity: number;\n  allowDuplicates: boolean;\n  basePolarities: Polarity[]; // The polarities natively on the weapon\n}\n```\n\n### 2. Capacity Validation Math\nWe will introduce a greedy bin-packing algorithm to calculate the cheapest possible cost of a build given the weapon's polarities.\n\n#### [NEW] `src/lib/engine/optimizer/capacity.ts`\n- Implement `calculateBuildCost(build: ParsedMod[], polarities: Polarity[]): number`.\n- **Logic**:\n  1. Sort the `build` mods by `capacityCost` descending.\n  2. Maintain a list of available `polarities`.\n  3. For each mod, try to find a matching polarity in the available list.\n  4. If a match is found, add `Math.ceil(mod.capacityCost / 2)` to the total cost and consume that polarity slot.\n  5. If no match is found, add `mod.capacityCost` to the total cost.\n  6. Return `totalCost`.\n\n### 3. Generator Constraint Enforcement\nThe generator must ensure that only legal builds are passed into the pipeline.\n\n#### [MODIFY] `src/lib/engine/optimizer/generator.ts`\n- Update `generateR
Version 32: # Phase 7: Meta Validation Pass & Mechanics Fixes\n\nBefore adding more complex mechanics or advanced search algorithms, we must rigorously validate that the Engine understands the core game mechanics properly. The 116% anomaly highlighted a blind spot in our Status Rules and Scoring Profiles. This phase will patch the engine math, introduce objective profiles, and pit the engine against 5 archetypal Meta builds to flush out any remaining mathematical errors.\n\n## Proposed Architecture\n\n### 1. Fix Slash Crit Inheritance\nWarframe Status procs (like Slash, Heat, Toxin) inherit the damage of the hit that caused them, including the Critical Damage multiplier. Hunter Munitions exclusively triggers on Critical Hits, so its procs must inherit critical damage.\n\n#### [MODIFY] `src/lib/engine/formulas/statusRules.ts`\n- Update `calculateSlashProcDamage` to accept an optional `critMultiplierOverride`.\n- If provided, the Slash tick damage will be multiplied by the `critMultiplierOverride`.\n- For `Hunter Munitions`, we will pass the weapon's `critMultiplier` (or the average expected multiplier of critical hits if `critChance > 1`).\n\n#### [NEW] `tests/status_crit.test.ts`\n- **Regression Test**: Evaluate Hunter Munitions damage on a weapon with `Point Strike` + `Vital Sense` vs a weapon with just `Point Strike`. Assert that the `statusDPS` output correctly scales with the critical damage multiplier.\n\n### 2. Introduce Core Score Profiles\nA single `RAW_DPS_PROFILE` cannot evaluate hybrid weapons fairly. We need specific lenses through which to judge builds.\n\n#### [MODIFY] `src/lib/engine/optimizer/scoreProfile.ts`\n- Add `HYBRID_PROFILE`: `{ directDPS: 1.0, statusDPS: 1.0, statusApplicationRate: 0.5 }`.\n- Add `STATUS_PROFILE`: `{ statusDPS: 1.5, directDPS: 0.2, statusApplicationRate: 1.0 }`.\n\n### 3. Re-run Braton Prime Benchmark\nWe will rewrite the Braton benchmark to use the `HYBRID_PROFILE` and observe if the Optimizer correctly values `Hunter Munitions` against a Level 100 Grineer target with 300 Armor.\n\
Version 33: # Phase 8: Stacking State Context\n\nThis phase will build the foundational architecture for complex combat synergies. The central goal is to expand the mathematical engine's awareness of the current combat environment, specifically \"How many unique status effects are on the enemy?\" and \"How many times has this stacking buff triggered?\"\n\n## Proposed Architecture\n\n### 1. Update `CombatState`\nWe will expand the contextual state passed to the engine.\n\n#### [MODIFY] `src/lib/engine/types.ts`\nExpand `CombatState` to include new stack parameters:\n```typescript\nexport type CombatState = {\n  statusTypesOnEnemy?: number;\n  galvanizedStacks?: number;\n  mercilessStacks?: number;\n  deadheadStacks?: number;\n  adaptationStacks?: number;\n  comboMultiplier?: number;\n};\n```\n\n### 2. Implement Condition Overload Family\nCondition Overload, Galvanized Aptitude, Galvanized Savvy, and Galvanized Shot all share the exact same mechanical archetype: `+Damage per unique status effect`.\n\n#### [NEW] `src/lib/engine/evaluator/effects/ConditionOverloadFamily.ts`\n- Register `EffectType.CONDITION_OVERLOAD`.\n- Retrieve `context.evaluationContext.combatState.statusTypesOnEnemy`.\n- Calculate `bonusDamage = modifier.parameters?.value * statusTypesOnEnemy`.\n- Return a `Modifier` stat change that injects `bonusDamage` as a standard `baseDamage` multiplier directly into the math engine.\n\n### 3. Implement Galvanized Stack Engine\nGalvanized mods have an on-kill mechanic that stacks up to a maximum limit (e.g., +30% Multishot on Kill, stacks up to 5x).\n\n#### [NEW] `src/lib/engine/evaluator/effects/GalvanizedStacks.ts`\n- Register `EffectFamily.GALVANIZED`.\n- Retrieve `context.evaluationContext.combatState.galvanizedStacks` (defaults to max stacks if `mode === \"MAX_STACKS\"`).\n- Multiply the parameter's `value` by the `galvanizedStacks` (capped at `modifier.parameters.maxStacks`).\n- Inject the resulting stats (`multishot`, `statusChance`, etc.) into the math engine.\n\n### 4. Parser Support for the New Mechanics\nWe
Version 34: # Phase 9: Melee & Stance Foundation\n\nThe goal of this phase is to expand the evaluation engine to natively support Warframe's most crucial melee scaling vectors: Combo Multiplier and Combo Count. By integrating these systems, we can natively simulate the two most iconic melee scaling mods—Blood Rush and Weeping Wounds—using the exact same `ScalingEffect` architecture developed in Phase 8.\n\n> [!NOTE]\n> This validates the generic `ScalingEffect` structure. If we can hook `Blood Rush` through the exact same logic pipeline as `Condition Overload` and `Galvanized Chamber`, the architecture is highly sound.\n\n## Proposed Changes\n\n### `src/lib/engine/types.ts`\n- **[MODIFY]** Expand `CombatState` to include `comboMultiplier?: number` and `comboCount?: number`.\n\n### `src/lib/engine/evaluator/effects/ScalingEffect.ts`\n- **[MODIFY]** Update the `ScalingEffect` evaluator to intercept `ScalingSource.COMBO_MULTIPLIER`. \n- Implement the Warframe standard math for combo scaling: `units = Math.max(0, comboMultiplier - 1)`. (e.g., a 12x combo provides 11 stacks of Blood Rush).\n\n### `src/lib/engine/evaluator/comboEstimator.ts`\n- **[NEW]** Create a dedicated `comboEstimator.ts` (paralleling `statusEstimator.ts`). \n- Default Heuristics:\n  - `FRESH_TARGET`: `comboMultiplier = 1`\n  - `SUSTAINED_FIRE`: `comboMultiplier = 8` (to represent prolonged combat build-up)\n  - `MAX_STACKS`: `comboMultiplier = 12`\n\n### `src/lib/engine/parser/registry.ts`\n- **[MODIFY]** Add Regex parsing patterns for:\n  - `Critical Chance stacks with Combo Multiplier` -> Maps to `ScalingSource.COMBO_MULTIPLIER` affecting `ModifierStat.CRIT_CHANCE`\n  - `Status Chance stacks with Combo Multiplier` -> Maps to `ScalingSource.COMBO_MULTIPLIER` affecting `ModifierStat.STATUS_CHANCE`\n\n## Verification Plan\n\n### Automated Tests\n1. Update `tests/scaling_effect.test.ts` to assert that `Blood Rush` correctly parses into the `ScalingEffect` flow.\n2. Assert that `EvaluationMode` directly controls the expected mathematical output of Blood Rus
Version 35: # Phase 9B: Heavy Attacks & Combo Efficiency\n\nThis phase expands the melee engine to accurately model Heavy Attack mechanics, which operate on entirely different mathematical assumptions than sustained DPS builds.\n\n## Proposed Changes\n\n### 1. New Core Stats\n*   **`ModifierStat` Enhancements**:\n    *   Add `INITIAL_COMBO` to track base combo floor.\n    *   Add `HEAVY_ATTACK_DAMAGE` to track explicit multipliers for heavy attacks.\n    *   *Note: We already have `HEAVY_ATTACK_EFFICIENCY` which directly models Combo Efficiency.*\n\n### 2. `CombatState` & Engine Types Evolution\n*   **`ComboState` updates**:\n    *   `ComboState = { count: number; multiplier: number; initial: number; }`\n*   **`HeavyAttackState` Addition**:\n    *   Add `heavyAttackState?: { comboConsumed: number; comboRemaining: number; }` to `CombatState` to explicitly model combo dumping.\n*   **`CalculatedWeapon` updates**:\n    *   Add `initialCombo`, `heavyAttackEfficiency`, `heavyAttackWindUp`, and `heavyAttackDamageMultiplier` (or similar) into the final weapon shape.\n\n### 3. Metric Generation\n*   **`WeaponMetrics` Updates**:\n    *   Add `heavyAttackDamage: number` (calculates the explicit damage of a heavy swing, considering combo consumption and heavy multipliers).\n    *   Add `comboRetention: number` (calculated from efficiency, caps at 90%).\n*   **`metricsExtractor.ts`**:\n    *   Add calculation logic to estimate the burst damage of a single heavy attack using the resolved `ComboState` and `HeavyAttackState`.\n\n### 4. Optimizer Profile\n*   **`scoreProfile.ts`**:\n    *   Create a dedicated `HEAVY_ATTACK_PROFILE` focusing purely on front-loaded burst.\n    *   Proposed weights: `heavyAttackDamage: 1.0`, `directDPS: 0.2`, `statusDPS: 0.1`, `comboRetention: 0.8`.\n\n## Verification Plan\n\n### Automated Tests\n1. **Initial Combo Verification**: Assert that `Corrupt Charge` (+30 Initial Combo) forces the evaluator's `FRESH_TARGET` combo multiplier to immediately begin at `2x` instead of `1x`.\n2. **Combo Consumption**: Asse
Version 36: # Phase 10: Enemy Profile System\n\nThis phase replaces the generic enemy mock data with a dedicated `EnemyProfile` system, allowing the optimizer to evaluate Time-to-Kill (TTK) metrics against specific endgame threats.\n\n## Proposed Changes\n\n### 1. `EnemyProfile` Architecture\n#### [NEW] `src/lib/engine/enemy/enemyTypes.ts`\n*   Define the `EnemyProfile` struct:\n    ```typescript\n    export type EnemyProfile = {\n      id: string;\n      name: string;\n      faction: FactionType;\n      level: number;\n      health: number;\n      shields: number;\n      armor: number;\n      statusImmunities?: DamageType[];\n      damageReduction?: number; // E.g., Acolyte attenuation or boss DR\n    };\n    ```\n\n#### [MODIFY] `src/lib/engine/types.ts`\n*   Update `EvaluationContext` to use `EnemyProfile` instead of an inline enemy definition.\n\n### 2. Enemy Presets\n#### [NEW] `src/lib/engine/enemy/enemyPresets.ts`\n*   Define standard benchmarks using the new `EnemyProfile` type. Since I do not currently have the exact scaling formulas or base stats for the factions, I will use placeholder health/armor values representing roughly level 100-200 enemies for:\n    *   Level 100 Grineer Heavy Gunner\n    *   Level 200 Steel Path Heavy Gunner\n    *   Corpus Tech\n    *   Eximus\n    *   Acolyte\n\n### 3. TTK Metric implementation\n#### [MODIFY] `src/lib/engine/optimizer/metricsTypes.ts`\n*   Add `timeToKill: number` to `WeaponMetrics`.\n\n#### [MODIFY] `src/lib/engine/optimizer/metricsExtractor.ts`\n*   Calculate Effective Health Pool (EHP). As a V1 approximation before full armor mitigation is implemented:\n    *   `EHP = shields + health * (1 + armor / 300)`\n*   Calculate `timeToKill = EHP / sustainedDPS`. If `sustainedDPS` is 0, TTK is Infinity.\n\n## Open Questions\n\n> [!IMPORTANT]\n> **Faction Details & Scaling Math**\n> You mentioned pulling faction details from the git repo, but the `database_builder` folder from `warframe-items` does not seem to contain enemy data dumps (only Weapons, Mods, Warframes, etc.).\n>
Version 37: # Phase 11: Enemy Damage Simulation\n\nThe goal of this phase is to transition the optimizer from pure theoretical DPS scoring to practical, enemy-specific TTK scoring. By implementing damage-type multipliers and simulating core status effects, the optimizer will accurately reflect real Warframe mechanics (e.g. why Viral/Slash excels against armored Grineer).\n\n## Proposed Changes\n\n### 1. Damage Type Multipliers\n#### [NEW] `src/lib/engine/enemy/damageTypes.ts`\nWe will encode the Warframe damage modifier tables into standardized lookup functions.\n\n*   Update `HealthType`, `ArmorType`, and `ShieldType` to match standard uppercase formatting (`CLONED_FLESH`, `FERRITE`, etc.).\n*   Implement `getHealthModifier(damageType, healthType)`\n*   Implement `getArmorModifier(damageType, armorType)`\n*   Implement `getShieldModifier(damageType, shieldType)`\n\n### 2. Damage Vector Resolution\n#### [NEW] `src/lib/engine/enemy/damageResolution.ts`\nWe will create `resolveDamageAgainstEnemy(damageVector, enemy, activeStatusModifiers)`.\nThis function will iterate through the damage vector and compute `effectiveDamage` using the standard Warframe formula:\n`Damage = Base_Damage * (1 + Health_Modifier) * (1 + Armor_Modifier) / (1 + (Armor * (1 - Armor_Modifier)) / 300)`\n\n### 3. Core Status Effects Integration\nWe will add support for the \"Big Four\" statuses to the damage resolution layer:\n*   **Viral**: Applies a multiplier to health damage based on estimated stacks (up to +325% at 10 stacks).\n*   **Corrosive**: Reduces the target's total armor before mitigation calculations (up to 80% reduction at 10 stacks).\n*   **Heat**: Reduces the target's total armor by 50%.\n*   **Slash**: Calculates bleed DPS that completely bypasses armor calculations.\n\n### 4. Metrics Update\n#### [MODIFY] `src/lib/engine/optimizer/metricsTypes.ts`\n*   Add `effectiveDPS: number;` to `WeaponMetrics`.\n\n#### [MODIFY] `src/lib/engine/optimizer/metricsExtractor.ts`\n*   Update `metricsExtractor.ts` to output `effectiveDPS` by combining `res
Version 38: # Phase 12: Arcane Integration\n\nWe have successfully extracted all 168 Arcanes from `warframe-items` and built `arcanes.db` (containing `uniqueName`, `name`, `type`, `rarity`, and `levelStats`). \n\nThe next step is to seamlessly integrate these Arcanes into our evaluation engine so that they function exactly like Mods but utilize the `EffectRegistry` to apply their scaling conditions (e.g. Primary Merciless).\n\n## Proposed Architecture\n\n### 1. Arcane Types & Context\n#### [MODIFY] `src/lib/engine/types.ts`\n*   Add `Arcane` to the global types (mirroring the Mod interface but containing its max-rank stats or specific rank).\n*   Add `arcanes: Arcane[]` to the `EvaluationContext` so the evaluator knows which Arcanes are equipped.\n\n### 2. Arcane Parsing Layer\n#### [NEW] `src/lib/engine/parser/arcaneParser.ts`\nWe will implement an `extractArcaneEffects(arcane, rank)` function.\nSince Arcanes primarily provide conditional scaling effects rather than flat static stats, we will map popular offensive Arcanes directly to the `EffectRegistry`.\n\n*   **Primary/Secondary Merciless:** Maps to `ScalingEffect` -> `baseDamage` triggered by `ScalingSource.ON_KILL` (up to 12x or 360%).\n*   **Primary/Secondary Deadhead:** Maps to `ScalingEffect` -> `baseDamage` triggered by `ScalingSource.ON_HEADSHOT` (up to 3x or 360%).\n*   **Primary/Secondary Dexterity:** Maps to `ScalingEffect` -> `baseDamage` triggered by `ScalingSource.ON_MELEE_KILL` (up to 6x or 360%).\n*   **Cascadia Flare:** Maps to `ScalingEffect` -> `baseDamage` triggered by `ScalingSource.ON_HEAT_PROC` (up to 480%).\n\n### 3. Pipeline Integration\n#### [MODIFY] `src/lib/engine/optimizer/pipeline.ts`\n*   The `evaluateBuild` function will pull the Arcanes provided in the Loadout/Context.\n*   Pass the equipped Arcanes through the `arcaneParser`.\n*   Feed the resulting `Modifier[]` or `Effect[]` into the `calculateWeapon` engine.\n\n## Open Questions\n\n> [!IMPORTANT]\n> **Arcane Rank Handling**\n> Should we assume all Arcanes are Max Rank for the initial V
Version 39: # Phase 12: Endgame Weapon Ecosystem\n\nThis phase integrates the core endgame scaling mechanics that define modern Warframe builds: Arcanes and Galvanized Mods. By translating these into our established `ScalingEffect` architecture, the optimizer will be able to evaluate raw damage vs status damage builds in context.\n\n## Proposed Changes\n\n### 1. New Scaling Sources\n#### [MODIFY] `src/lib/engine/types.ts`\nWe will expand the `ScalingSource` enum to handle specific Arcane and Galvanized mod trigger conditions:\n*   Add `KILL_STACKS`\n*   Add `HEADSHOT_KILL_STACKS`\n*   Add `STATUS_TYPES_ON_ENEMY`\n\n### 2. State Estimation\n#### [MODIFY] `src/lib/engine/evaluator/statusEstimator.ts`\nWe will create `estimateKillStacks(mode, context)` and `estimateHeadshotStacks(mode, context)`.\n*   `FRESH_TARGET`: 0 stacks\n*   `SUSTAINED_FIRE`: Estimated heuristic (e.g. 10x Merciless, 2x Deadhead) based on combat density.\n*   `MAX_STACKS`: Maximum possible stacks.\n\n### 3. Arcane Parser & Registry (12A & 12B)\n#### [NEW] `src/lib/engine/parser/arcaneParser.ts`\nWe will build a simple parser that maps Arcane data from `arcanes.db` into the `EffectRegistry`:\n*   **Primary/Secondary Merciless**: Maps to `ScalingEffect` on `BASE_DAMAGE` utilizing `ScalingSource.KILL_STACKS` (Up to 12x, +30% each).\n*   **Primary/Secondary Deadhead**: Maps to `ScalingEffect` on `BASE_DAMAGE` utilizing `ScalingSource.HEADSHOT_KILL_STACKS` (Up to 3x, +120% each).\n\n### 4. Galvanized Status Scaling (12C)\n#### [MODIFY] `src/lib/engine/parser/modParser.ts`\nWe will expand the mod parser to identify \"Galvanized Aptitude\", \"Galvanized Savvy\", and \"Galvanized Shot\", mapping them to a `ScalingEffect` on `BASE_DAMAGE` utilizing `ScalingSource.STATUS_TYPES_ON_ENEMY` (which leverages the `expectedUniqueStatuses` we built in Phase 9).\n\n## Verification Plan\n1. **Tests for 12A**: Equip Primary Merciless and verify it correctly grants +360% base damage under `MAX_STACKS`.\n2. **Tests for 12C**: Equip Galvanized Aptitude against an enemy with 4 ex
